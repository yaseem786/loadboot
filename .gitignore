__pycache__/
*.pyc
loadboot_site.zip
site/
node_modules/
PHASE-2A-v*.diff
COMMITS.txt
scripts/staging-apply.log
scripts/staging-reset.log
scripts/logs/
loadboot-phase2a-v*.zip

# Local-only secret (copy into Supabase, then delete). Never commit.
VAPID_PRIVATE_KEY.local.txt

# GA service-account private key — NEVER commit
ga-service-account.json
*service-account*.json
*.ga-key.json

# Cowork device-bridge workaround: git lock files moved here (bridge cannot unlink)
_to_delete/

# Play upload keystore — NEVER commit
loadboot-upload.keystore
PLAY-KEYSTORE-NOTE.txt

# Session staging area (device bridge) — never commit
_stage/

# iOS shell (tools/ios): generated Xcode project + deps — regenerated in CI, never commit
tools/ios/ios/
tools/ios/node_modules/
tools/ios/package-lock.json.bak
# Apple secrets — NEVER commit
*.p8
AuthKey_*.p8

# Session scratch + personal docs (never commit)
Claude outputs/
docs/apple/*.docx

# Phase 2 SEO crawl: regenerable (harness/seo-crawl.py), 600 KB per run - keep csv/report, not the json
docs/seo-audit-2026-10/data/crawl.json
