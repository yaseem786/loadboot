# Partner 360 v2 — Broker · Broker Agent · Shipper (bl_bp_0455, 26 Sep 2026)

Owner ask (26 Sep): the Partners directory "Packet" pill was a text blob, one broker-flavoured 360 served agents and
shippers, and CC could not answer "where is this account stuck / what fired / what do I do next" without opening four
screens. Wanted: three separate premium 360s (carrier-360 standard or better), onboarding of all three verified end to
end (checks & balances, emails, in-app), supply side to DAT / Uber / Amazon Relay standard, CC <-> portal linkage.

## Shipped (milestone 1) — branch `claude/eloquent-maxwell-iqsqvw`, merge into `main`

DB — `migrations/bl_bp_0455_partner_360_v2.sql`, applied staging + prod, anon SECDEF 36/35 by name unchanged:
- `app_private.partner_role(org)` -> broker | agent | shipper | facility.
- `app_private.partner_journey(org)` -> the onboarding ladder per role (account -> email -> FMCSA screen -> identity /
  brokerage declared -> screened -> confirmed / business check -> company email -> agreement -> first post -> packet ->
  approved), each step done / current / blocked with timestamp + staff-facing detail + action key; stage label + tone,
  next_action, blockers. Same facts the portal trust card shows the customer.
- `public.cc_partner_360(org)` v2 — role-aware; v1 keys kept. Adds owner/members, trust (screening, identity,
  posting allowance, parents, invites / shipper domain check, company email), journey, packet + summary (expiring,
  lapsed), agreements (+ what is published), loads with board/trip/offers/claims, load_stats, shipments (shipper),
  offers, invoices, pay (0452), sla, brokerage (agents under a broker), comms (emails from message_deliveries, in-app
  notices, blocked sends, staff alerts), violations, tasks, update_requests_open, timeline 40.
- `public.cc_partners_accounts()` v2 — adds role, tier, stage, stage_tone, next_action, can_post, journey done/total,
  blockers, owner_email, last_sign_in_at, email_confirmed, loads_30d, unread_notices, is_demo.

CC (ships with the next site push):
- `views/partner360-kit.js` — hero, next-action banner, 6-tile KPI row, sticky section nav, journey ladder, packet
  review (verify / waive / reject+warn / remind, W-9 + agreement print — lifted from v1), bank card, approve / park
  gate, trust actions (askReason / askConfirm, no prompt()), comms (Emails · In-app · Blocked · Staff alerts), health,
  people, timeline, 30 s live refresh while the tab is visible + "refresh now".
- `views/broker360.js` (dispatcher + Freight Broker screen), `views/agent360.js`, `views/shipper360.js`.
  Routes `#/broker`, `#/broker-agent`, `#/shipper` (`?id=`) all dispatch on the server role; stale links still work
  and the hash is normalised.
- `views/partners.js` — Account · Stage · Next for staff · Journey · Loads (30 d / total) · Last sign-in · Status;
  filters All / Brokers / Broker agents / Shippers + "Needs attention"; sorted needs-attention first; KPIs: accounts,
  need a human, packet items to review, signed in this week.
- `app/shared/ui/entityLink.js` — `partnerHref / orgHref / orgHrefFor / carrierHref`; wired into brokerTrust (shipper
  rows now have a 360 link), accountHealth, deletionRequests, contactsDirectory (was dropping the partner id),
  partnerCompliance, brokerSla (rows open the broker's 360, not the directory tab).

Verified: headless Chromium render of all three screens + the directory from staging's real cc_partner_360 output —
no runtime errors; screenshots reviewed (KPI grid 6-up, pills no longer auto-capitalised, long "why blocked" text moved
out of the facts grid).

## Findings that need the owner (not code)

1. **`broker_shipper` agreement is NOT published on prod (or staging).** Every shipper's "Shipper Agreement" step is
   blocked and the portal copy "one click when published" is unfulfilled; the `signed_agreement` packet item falls to a
   PDF upload. Publish it under CC -> Legal / agreements (legal-approved), then `trg_agreement_fills_packet` auto-verifies
   the item. Two real shippers (Sourcing Advisory Group, SOFTBANK ROBOTICS AMERICA) are sitting on exactly this.
2. **LinkLane** reads "Confirmed agent — Approve account now" — one click in Broker Agent 360.
3. **khannawab m afzal** — declared MC not found on FMCSA; 0449 nudge went out; needs a human (re-screen or pass by hand).

## Next milestone — onboarding gaps found by the audit (priority order; all main-loop)

G1 Shipper portal: `appView` sends shippers to `brokerDash` (app.js:5416); `mountShipperTrust` (company-email code form,
   re-check, ladder) only mounts in the unused `shipperDash`. A free-mail shipper has NO UI to verify a company email;
   a business_verified shipper cannot reach the request form until the 3-item packet is verified (contradicts the card
   copy + the in-app "request your first quote"). Fix: mount the shipper trust card in brokerDash for shippers and
   unfold the form on `can_post`, not `onboarded`.
G4 No JS calls `cc_accept_agreement('broker_shipper')` — add the click for shippers (needs finding 1 first).
G6 Agent welcome is broker-flavoured (in-app "Screen your MC…", staff "NO MC GIVEN", `welcome.broker` 3-step). Add a
   `welcome.broker_agent` catalog row + branch in `trg_partner_org_welcome` / `cc_partner_register` (email rule §6).
G7 Authority pause (`bl_bp_0343`) alerts staff but never the broker (only on restore). Add partner in-app + a catalogued
   `broker.authority_paused` email.
G8 Phone identity verification lost its staff notice in the 0318 rewrite — re-add `broker.verified_by_phone`.
G9 Screening dead-ends (unknown / not_found / error; shipper free_mail / error) create no partner notice; "FMCSA lists no
   email" promises a call but creates no staff notice.
G13 Staff notice URLs: `partner.registered` and `shipper.checked` point shippers at `#/broker?id=` / `#/broker-trust` —
   use `#/shipper?id=`.
G2 Shipper-posted loads bypass `trust_label_load` / `enforce_trust_gate_not_bookable` (broker-only) — verify live, then
   extend to shippers.

## Later milestones
- Partner portal deep links: `#tab/target` + `?param` parsing (carrier-style) so CC / emails can land on
  `#onboarding/w9`, `#loads/<id>`; CC "open in portal" buttons then become real.
- Real-time: `app_private` is not in the realtime publication, so the 360s poll (30 s). A `partners:live` broadcast
  from `notify_partner` / trust changes would make it instant (same pattern as `dispatch:live`).
- Supply-side board polish (DAT / Relay parity) is a separate piece — needs its own audit of `app/partner/app.js`
  post-load wizard + carrier board card.
