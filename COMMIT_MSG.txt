Play app live: site Play badge + privacy rewrite + broker/tablet store shots; workstream 02 industry-pages module

Google Play (LoadBoot Load Board & Dispatch, com.loadboot.app) went live 26 Aug.
This commit closes the listing<->website<->app gaps from docs/PLAY-LISTING-AUDIT-2026-08-28.md
and lands the other session's workstream-02 (shipper-by-industry) module.

Site (build_site.py)
- /apps: "Now available on Google Play" + badge; "coming soon" scoped to the App Store;
  Android card links to the listing, PWA install kept as fallback.
- Footer on every page: Google Play badge + one-line iPhone note.
- /privacy: full rewrite dated 28 Aug 2026 — covers loadboot.com, all portals and the
  Android app; carrier / broker & shipper / agent data sections; location consent;
  sharing between parties on a load (user-directed) so it matches the Play Data safety
  declaration; retention; deletion via Settings or privacy@loadboot.com.
- /free-load-board-for-brokers: "Post by email — loads@loadboot.com" and
  "Post from your phone" cards.  /integrations#email: email-posting section.
- "native apps in preparation" wording updated in the features list.

App shells
- app/shared/ui/playInstall.js (new): Android-only, never inside the installed app
  (standalone / android-app://com.loadboot.app referrer), 14-day dismissible
  "Get LoadBoot on Google Play" pill. Included in carrier, partner, agent index.html
  and the /app/ chooser.
- google-play-badge.svg (new, root): self-drawn badge; swap for the official asset later.

Store assets (docs/playstore-screenshots/)
- broker-01..04 (1080x1920), tablet7-01..03 (1200x1920), tablet10-01..03 (1600x2560):
  marketing frames built from live partner-portal captures + the carrier dashboard.
  Uploaded to Play; listing update (new short description, 8 phone shots incl. 3
  broker, 7" + 10" tablet shots) submitted for review 28 Aug. Tag "Vehicle maintenance"
  removed.

Docs
- docs/PLAY-LISTING-AUDIT-2026-08-28.md: the cross-match audit that drove the above.

Workstream 02 (other session, same day)
- industry_pages_module.py, apply_workstream02.py, docs/WORKSTREAM-02-build_site.diff:
  shipper-by-industry pages module + the patch that wires it into build_site.py.
  NOTE: build_site.py was edited by the Play session at 12:24; the workstream-02 diff
  was produced at 18:37 — re-check the diff applies cleanly before running apply_workstream02.py.

Prod data (no code): demo brokerage/shipper loads re-dated to 29 Aug–3 Sep and
app_private.partner_loads seeded (10 rows) so the reviewer/demo accounts show a live
board. Demo-only visibility. They expire again ~3 Sep.
