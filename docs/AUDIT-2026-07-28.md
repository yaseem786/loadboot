# LoadBoot FULL DEEP AUDIT — register (started 2026-07-28)

Owner order: crawl every page + portal click-through at Amazon/Uber bar; log every minor bug.
Status: **round 1 done** (security sweep + static gates + live marketing-site pass).
Round 2 (pending): logged-in click-through of all 4 portals on staging (lb.test personas) + mobile pass.

## FIXED THIS SESSION
| # | Severity | Area | Finding | Status |
|---|----------|------|---------|--------|
| 1 | CRITICAL | DB security | anon EXECUTE drift on public SECURITY DEFINER fns: staging 143 / prod 89 (direct grants incl. staff cc_assign_load, cc_dispatcher_*, cc_create_invoice, my_webhook_*) | ✅ FIXED bl_sec_0190 both DBs → 26-fn allowlist; new check number = 26 |
| 2 | HIGH | Email-loads booking | No broker notification path existed for booking requests on email-sourced loads (v_broker null → silent) | ✅ FIXED booking-ping (bl_ping_0191/0192, load-mail v7, broker-ping.html) |
| 3 | LOW | Carrier UX | cc_request_book_load told carrier "broker has 30 minutes" even for email loads with a 4h hold | ✅ FIXED bl_ping_0192 (real window in message + return) |

## OPEN FINDINGS (round 1)
| # | Severity | Area | Finding | Suggested fix |
|---|----------|------|---------|---------------|
| 4 | MED | Tooling gate | scripts/check_imports.py FAILS on partnerIntake.js: ccAskReschedule used via dynamic import at line ~257 — false positive breaks the gate for every future session | teach check_imports about `await import(...)` destructuring, or convert to static import |
| 5 | LOW | Marketing home | Live-rates band + load-opportunities render "loading…"/"—" pre-JS; fine when JS runs but worth verifying get_public_market_rates + get_public_load_opportunities actually populate for anon after the 0190 grant restore | manual browser check (round 2) |
| 6 | LOW | FAQ page | Duplicate consecutive "Details on integrations" links (ELD answer); contact-card label style inconsistent ("Chat — instant, 24/7" vs "Chat") | copy pass in faq_module.py |
| 7 | LOW | CC routes | /exceptions route dead (duplicate key — exceptionCenter wins); exceptions.js unreachable legacy (known since Carrier 360 v2) | delete exceptions.js + route entry |
| 8 | INFO | Repo hygiene | .git/index.lock present on owner machine — commits will fail until owner deletes it (sandbox cannot) | owner: del .git\\index.lock |
| 9 | INFO | Edge hardening | load-mail calls RPCs with apikey-only header (no Authorization Bearer) → lb_email_load_ingest/lb_email_reply_merge must stay anon-granted; hardening would shrink anon surface by 2 | add Bearer SVC header in load-mail v8 + revoke + E2E retest |
| 10 | INFO | Staging env | staging edge fns missing GEMINI_API_KEY (load-mail can't parse on staging) + RESEND_API_KEY absent (sends no-op) — fine for now, remember when testing | add keys if staging E2E needed |

## ROUND 2 PLAN (portal click-through)
1. Carrier portal (staging, carrier-owner@lb.test): onboarding wizard end-to-end, dashboard cards incl. NEW Profile Strength ring + micro-asks, Load Board tabs, trip card + live map, docs, account tabs, payments — desktop + Ctrl+Shift+M mobile.
2. Partner portal: post-load wizard (rate card required fields), booking requests approve/reject, tracking.
3. Command Center: every nav view renders w/o console errors; Email loads queue; Match Center (new prefs_fit factor); Carrier 360 (new prefs fields + DAT banner); Dispatchers SOP (prefill).
4. Agent portal: signup → onboarding → referral link.
5. Marketing: every page in sitemap + noindex pages, console errors, broken images, mobile layout.

| 11 | LOW | Live chat concierge | Pre-role intro (lc_kb #70) said "I verify your authority" — carrier-only copy shown to everyone; likely bounced a real shipper lead Jul 27 | ✅ FIXED in-place both DBs (role-neutral intro; role pitches after selection already correct) |


## ROUND 2 RESULTS (2026-07-28, live click-through via Chrome on localhost build b1785248315 + staging DB)
**Carrier portal (carrier-owner@lb.test):** PASS — login, dashboard, Profile Strength ring card (15%, "8 missing" badge, chips); card click → LinkedIn-style checklist (equipment auto-detected "Added" from account data); lanes popup: add chip → Save → checklist refresh 15%→30%, ring card updates live; reopen shows PREFILL (saved lane chip). Zero console errors.
**Command Center (dispatcher@lb.test):** PASS — carrier correctly BLOCKED ("No staff access"); staff login OK; Email loads view full render (stats strip, tabs, broker rows, missing-field column); Carrier 360 prefs card shows ALL new fields (radius/home-time/load-size/min-total-rate/facility/DAT/Truckstop) and the carrier-saved lane appeared instantly. Zero console errors.
**New findings:**
| 12 | INFO | Staging fixtures | dispatcher@lb.test password had drifted — reset to LbShots2026! (staging only) | ✅ fixed |
| 13 | LOW | CC routes | No direct #/match-center route (falls back to Dashboard) — matchCenter.js reachable only via load actions; fine, but deep-link would help dispatchers | optional |
**Remaining for a later pass:** Partner + Agent portal click-through, marketing-site mobile pass, checklist/popup mobile (Ctrl+Shift+M) visual check.
**Test residue:** fixture carrier now has preferred lane "Dallas, TX → Atlanta, GA" (useful for matching demos; remove via Account if unwanted). CC tab left signed in as dispatcher@lb.test — owner: sign back into carrier portal as carrier-owner@lb.test to continue carrier testing.
