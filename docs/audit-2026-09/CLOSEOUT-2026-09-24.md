# September 2026 audit — close-out (24 Sep 2026, Claude)

ChatGPT/Codex's subscription ended on 24 Sep. Its last PUBLISHED work is `d59d1b6` (22 Sep, git hygiene + 6-Sep branch
archive), already on `main`. Two commits it held locally were never pushed and cannot be recovered; nothing on GitHub
depends on them. Claude took over every open item of the ChatGPT lane (WORK-SPLIT-2026-09-22.md is retired).

## What closed today
| Item | Result | Evidence |
|---|---|---|
| Frozen-user upload message | lc-doc-check returns `error:"frozen"` + a plain sentence (staging v18, prod v16); chat onboarding shows it without "try again"; carrier/staff uploads (`app/shared/storage.js`) translate the RLS refusal via `my_uploads_frozen()` | commit 5987bd9; contract test 45/45; esbuild OK; live smoke 401/400 unchanged |
| CC "Account deletion review" screen | Built by the parallel UX session as **bl_priv_0437** (Deletion desk, `views/deletionRequests.js`, staging+prod) on `cc_erasure_items` / `cc_erasure_decide` / `erasure-purge`; twins are handled server-side by bl_audit_0368 | commit 218551d |
| 0342/0343 review | Files stay STAGING-ONLY by design: their own guards refuse on prod (the functions already exist there). All 10 prod functions reviewed: every SECURITY DEFINER one is permission-gated; `fmcsa_webkey` is not executable by authenticated/anon. **One real bug found and fixed:** prod `cc_request_account_action('close')` closed the organization on the spot and mailed "press Reopen my account" — the portal has no reopen button and the confirm the carrier accepts says "Nothing is deactivated until you confirm". **bl_audit_0369** removes the silent auto-close (prod patched, staging no-op). Proven: old body → `closed=true, org closed`; new body → open request, org untouched, duplicate folded (4/4, rollback-txn). No carrier was affected (0 requests, 0 closed orgs at apply). | migrations/bl_audit_0369_*, tests/bl_audit_0369_rollback_test.sql; prod body md5 f7b2c838… |
| getToken (website chat uploads use the anon bearer) | ACCEPTED as designed: the marketing-site wizard is a guest flow keyed by `visitor_key`; the erasure freeze also matches the chat row's typed email (bl_audit_0356), so a frozen user is still stopped. Signed-in carriers upload from the portal Documents page. | 0356 test 13/13 |
| F19 bundle / images | The 2,072,328-byte `owner-operator-dispatch-hero.png` was published but referenced by no page (pages use the 100 KB `.jpg`) → moved to `docs/brand-kit/source/` so it is no longer deployed. Portal bundles (uncompressed): carrier app.js 845,994 B, partner 508,912 B, CC 32,065 B — splitting is a refactor, handed to the UX audit lane. Site: 482 files, 32.8 MB. | this commit |
| F20 Lighthouse | PageSpeed API quota exhausted on 24 AND 25 Sep, so Lighthouse 12 was run locally on the built homepage (plain http.server: no HTTPS/compression/cache, so those audits are artefacts). **Mobile: Perf 74 · A11y 95 · BP 79 · SEO 100 — FCP 2.4 s, LCP 5.3 s, TBT 100 ms, CLS 0.** **Desktop: 97 · 95 · 78 · 100 — LCP 1.2 s, CLS 0.005.** Real issues: (1) LCP element is the hero lead paragraph `p.lead.reveal` — the scroll-reveal fade holds the above-the-fold text back; exempt hero elements from `reveal`; (2) footer `logo-full-dark.png` 162 KB loads offscreen (lazy-load / WebP); (3) 11 colour-contrast failures on primary buttons (`.btn-primary`, `.lbh-go`, `.ls-btn.primary`); (4) heading order skips to h3; (5) `#pwaBtn` aria-label differs from its visible text. Handed to the UX/SEO lane (their Phase 2 already runs Lighthouse per page) — not fixed here to avoid a collision. Real-Chrome sample 24 Sep: TTFB 294 ms, 149 KB transferred. | local Lighthouse 25 Sep |
| F21–F23 (SEO / analytics) | Moved to the separate SEO lane (prompt given to Yaseen 22 Sep). | — |
| F24 cron load | Prod: 53 active jobs, 10 run every minute + the 30-second chat watchdog; 20,140 runs in 24 h, **0 failures**; DB 229 MB; `cron.job_run_details` 72,188 rows (≈3.5 days, already being trimmed). No action. | query 24 Sep |
| F26 status-page independence | VERIFIED 25 Sep from a real browser: loadboot.com is served through Cloudflare (`server: cloudflare`, `cf-ray` present) in front of Netlify, and the Cloudflare Worker's branded outage page `/__downpage` answers **503 "LoadBoot — We'll be right back"** — so an outage page exists outside Netlify/Supabase. Note: the hourly uptime monitor scheduled task is not in the account's cloud scheduled-task list (it may live in the desktop app); not verified. Also noticed: TWO weekly SEO scheduled tasks fire every Monday and both edit build_site.py titles — "Loadboot weekly seo loop" (24 Sep, 10:00 LA) and "LoadBoot weekly SEO loop (R-rounds)" (16:30 UTC) — flagged to Yaseen; not changed. | browser check 25 Sep |
| F27 style literals | Report-only; owned by the new A-to-Z UX audit (24 Sep). | — |

## Still open (not Claude's to finish)
- Yaseen: delete the 5 prod Storage test objects (list in HANDOFF 22 Sep; still present 25 Sep); attorney review of the F04 "not a broker" copy.
- Retell: enforce mode after credit + one clean signed delivery (inbound hook v3 logs `sig=` reasons).
