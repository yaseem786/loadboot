# Paste this to ChatGPT when its quota is back (after the Phase-1 review reply)

---

Continue. While you were out of quota, Claude built **all five Sprint 1 items on STAGING** (nothing on prod) and then did the Sprint 2 prep read-only: three new findings **F30–F32** are at the top of `PHASE1-AUDIT.md` (auto-FMCSA-at-signup is a silent no-op; `ai_verdict` is never written; the RPC drift is two staging-only migrations `wd_0032_per_trip_pnl_engine` + `wd_0033_carrier_cost_model_rpc` that prod never got). Do NOT redo any of it. Start exactly here:

1. **Re-sync header first** (main sha + new commits, `list_migrations` prod + staging, `list_edge_functions` both, changed files). Expect on staging: `bl_sec_0320_email_ingest_service_only`, `bl_bp_0321_agent_confirm_resend_idem` (superseded, rolled back), `bl_bp_0321b_agent_confirm_resend_idem_bigint`; edge fns `load-mail` v8 and `domain-check` v3 (verify_jwt=true). Expect prod unchanged. If that is not what you see, stop and tell me.
2. **Read `docs/audit-2026-09/HANDOFF.md` in full** — CURRENT STATE has the per-item verification evidence and the exact prod steps; NEXT ACTION tells you what "continue" means right now. Then read the new files (`migrations/bl_sec_0320_*.sql`, `migrations/bl_bp_0321_*.sql`, `supabase/functions/load-mail/index.ts` (v8 — source was never in the repo before), `supabase/functions/domain-check/index.ts` (v2), the F07 block in `build_site.py` ~lines 42–52, `scripts/check_imports.py`, `package.json`, `docs/audit-2026-09/pr-checks.yml`).
3. **Review Claude's work as a second pair of eyes** — you are the reviewer for this sprint. Look for anything that would break the real paths: the load-mail → RPC chain with the new guard, the pg_net → domain-check collector with verify_jwt on, the agent-confirm resend, the local build flow. Write findings as a short "Sprint 1 review" section appended to `PHASE1-AUDIT.md` (dated changelog line at top). If everything holds, say so explicitly — "no findings" is a valid result.
4. **Then do the NEXT ACTION in HANDOFF.md.** Right now that is: if I have said "apply to prod" in this thread → the Prod-step column, one item at a time, re-running each staging test on prod. Otherwise → **F32 port prep on STAGING only**: pull the two `wd_0032`/`wd_0033` statements from staging's `supabase_migrations.schema_migrations`, save as `migrations/bl_fin_0322_port_trip_pnl_engine.sql` with a header, diff every referenced table/column against PROD, write (not run) the rollback-txn test. F30 (collector-pattern reuse) and F31 (`doc_set_ai_verdict` RPC + persist verdict + doc-precheck source into repo) stay as design text in `PHASE1-AUDIT.md` until I approve Sprint 2.
5. **Content rule added:** every SEO/content item must be re-ranked against LIVE Search Console query/page data and GA4 before anything is written — never guessed keywords. Use the `seo-pull` recipe (Supabase `net.http_post` → poll `net._http_response`, `rowLimit:500`). Update the 90-day SEO calendar in `90-DAY-PLAN.md` accordingly when you get to it — and mark it as re-ranked with the data date.
6. Before you stop: rewrite CURRENT STATE, set NEXT ACTION, append your LOG line, commit on `audit/s1-boundaries` (create it if Claude could not — device shell was down on its side).

Two lessons from Claude's turn that you should carry: (a) an exact-key comparison in `domain-check` broke the real caller — `fmcsa_config.auth_key` is a valid anon JWT for this project but not byte-equal to the function's `SUPABASE_ANON_KEY` env; verify identity by verified claims (role + ref), not by string equality; (b) `verify_codes.id` is **bigint**, not uuid — the rollback-transaction test caught it before it reached prod. Keep testing that way.
