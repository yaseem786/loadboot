# Agent payout review — 2026-09-15

Three agent payout-review RPCs had the existing permission checks but no current-session check. The verify RPC also accepted NULL as a decision, wrote rejected status and returned success without requiring a rejection reason.

## Change and deployment

Staging migration `20260915055340 audit_agent_review_session_guard` is applied. Local migration file was generated by Supabase CLI at `supabase/migrations/20260915055134_audit_agent_review_session_guard.sql`; server-assigned history version differs.

The existing private session helper is called first. NULL decisions raise 22023; finance.approve/carriers.approve checks and true/false behavior remain. Each environment's exact source is patched under observed-hash guards, preserving production notification text. Grants and helper source remain unchanged.

Production deployment was NOT executed: automatic approval review rejected it, citing the need for explicit authorization for this exact production money-related migration. Do not retry via execute_sql or another route. Request approval for this concrete three-function package after handoff.

## Verification

- Staging source copies: 24 isolated assertions PASS.
- Production source copies executed in temporary staging objects: 24 isolated assertions PASS.
- All data dependencies, auth/permissions and email sender were replaced in these copies with pg_temp fixtures/stubs. No customer records or actual email queue were used. These are source-contract tests, not real-session end-to-end validation.
- Actual candidate RPCs: three missing-identity refusals PASS, transaction rolled back.
- Apply/rollback/reapply and three missing-identity refusals PASS, transaction rolled back.
- Deployed staging: three missing-identity refusals PASS.
- Deployed source and grants match exact expected transformations. Anonymous callable names/arguments unchanged at 32.
- All security advisor findings unchanged excluding observation timestamps; this does not mean the existing advisor findings are resolved.
- Prior current-session helper tests remain historical evidence; no new real-session positive integration/browser test is claimed.

No actual transfer, real account review, email, notification or provider call was made.

## Exact package and rollback

Source snapshot: AGENT-REVIEW-BEFORE-2026-09-15.json.
Staging evidence/hashes: AGENT-REVIEW-DEPLOYMENTS-2026-09-15.json.
Candidate SQL and migration contain identical guarded patches.
Rollback: ROLLBACK-AGENT-REVIEW-2026-09-15.sql; reverse patch accepts only exact observed original hashes after stripping this change. Run as an atomic migration if rollback is authorized; do not use to undo other lanes.

Function signatures:
- public.cc_agent_payout_approve_method(uuid,text)
- public.cc_agent_payout_request_details(uuid,text[],text)
- public.cc_agent_payout_verify(uuid,boolean,text)

## Faida aur baqi risk

Purani/revoked session se in teen review actions ka raasta band hota hai; khaali decision ghalti se rejection nahi banata. Yeh poore platform ki session protection ya complete audit closure nahi. Production par yeh teen functions abhi purane hain. Document/Storage access, erasure completion, F10 matrix decision, CI/browser/native checks and Retell gates remain open.

GitHub publishing stays held until all audit gaps are complete, per Yaseen's latest instruction. No public push or main deployment was performed.

Reference: [PostgreSQL CREATE FUNCTION](https://www.postgresql.org/docs/current/sql-createfunction.html) documents preservation of ownership/permissions with CREATE OR REPLACE; the migration also asserts the actual ACL.

