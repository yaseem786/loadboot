# LoadBoot — Independent review of `audit/carrier-legal-seo-hardening-v2` (5 Sep 2026)

Reviewer: Claude (Cowork session linked to `C:\Users\HP\Documents\GitHub\loadboot`).
Shared handoff file — either assistant (Claude / ChatGPT) continues from here when Yaseen types "continue".

## 0. What could and could not be inspected

- The branch `audit/carrier-legal-seo-hardening-v2` (commits `efc5f5d`, `6d50655`) lives in another workspace
  (`/workspace/scratch/fbda731c9ee2/loadboot-recovered`). It is **not** in Yaseen's local repo: `.git/refs/heads` holds only
  `main`, `feat/dispatcher-model`, `feat/responsive-ux-*`, `preview/command-center-v1`, `shots-3x-refresh`.
  `docs/OPERATING-MODEL.md`, `docs/CLAIM-REGISTER.md`, `docs/SEO-REMEDIATION-2026-09-04.md`, `tests/`, `.github/workflows/quality.yml`
  do not exist in the local folder.
- `device_bash` on Yaseen's machine failed to start (recurring since 25 Aug), so no local git/grep was possible.
- Therefore **no line of the branch was read**. Every code-level item below is UNKNOWN. What this review CAN do is check the
  proposed "binding operating model" and the described changes against what the production database actually does
  (from the project's own migration history and session records).

## 1. Verdict table

| Area | Verdict | Evidence | Remaining risk |
|---|---|---|---|
| Marketplace model (shipper self-post/self-select, no allocation) | PASS (model level) | Matches `docs/research/dispatch-compliance-operating-guide.md` (49 CFR 371.2: "allocation of traffic" = brokering); LoadBoot holds no broker MC (bl_bp_0312–0319 design); shipper loads go `partner_loads` → `cc_decide_partner_load` → board → request-to-book accepted by the poster. | CC still has `cc_match_rank` + broker/carrier offer tooling — fine as long as the poster accepts; the copy must not claim LoadBoot "matches" carriers for shippers. Code not read. |
| Dispatch agreement (LB-DSA-v3) | UNKNOWN / risk | File not readable. Version bump touches the packet: `org_onboarding_items` agreement item, `bl_bp_0316b_agreement_submit_keeps_verified`. | (a) Verify existing signed carriers (GABE LOGISTICS, Munster, Warren…) are not flipped to "agreement missing" by the new version string. (b) Carriers who signed the old text remain on old terms until re-acceptance — plan a re-accept prompt. (c) Removing ALL non-circumvention is a business decision, not a legal necessity; a narrow clause (introduced brokers only, 60–90 days) is industry-normal. |
| Payment model — 5% of gross linehaul, no custody | PASS (model level) | Pay rails are manual: `pay_transfers` kinds `freight|claim|platform_fee`, broker pays carrier/factor directly (`pay_instructions`, NOA/factoring engine bl_pay_0092–0096); LoadBoot never holds freight money. Agent engine bl_agent_0306 computes on `fin_invoices.gross`. | None on custody. |
| Payment model — fee invoiced ONLY after carrier is paid | **FAIL (as described)** | `bl_pay_0097`: fee invoices **auto-ISSUE `sent` on delivery** with in-app + email to the carrier; `auto_invoice_on_delivery` / `cc_create_invoice_core` create the fee invoice at `delivered`. The branch lists **no migration**, so the new agreement/Terms say "invoice after you are paid" while the system emails an invoice at delivery. | Either change the DB (issue at delivery but `due_at` = after carrier payment confirmed via `pay_confirm_received`) or change the wording to "invoice issued on delivery, payable only after you have been paid". Pick one; both is not allowed. |
| Accessorials excluded from fee base | UNKNOWN | `fin_invoices.gross` — whether it equals linehaul only or includes accessorial claims was not verified. | Query on staging: does `gross` include approved claim amounts from `claim_compute()`? If yes, the 5% is silently charged on detention/TONU too. |
| Agent/referral commission "1% of LoadBoot's 5%" | PARTIAL | Real engine: L1 1% + L2 0.5 + L3 0.25 + L4 0.15 + L5 0.10 = **up to 2% of gross**, 5 levels (`referral_levels`, bl_agent_0306). | `docs/OPERATING-MODEL.md` and any public copy must state the multi-level total, or the doc contradicts the ledger agents can see. |
| GPS / privacy — off by default, no auto-start | UNKNOWN / **high functional risk** | Old portal auto-started tracking at boot (`ensureLiveLoc` any-tab, `visibilitychange` fix). The delivery chain is GPS-driven: `cc_trip_arrive_gps` → `delivered` → `auto_invoice_on_delivery` → `fire_referral_accrue`; detention/layover/TONU (`detention_bill`, geofence exit) and the agent rule "1% only on COMPLETED (GPS-delivered) transactions". | Must prove on **staging** that a manual check-in reaches `delivered` and fires the invoice trigger. If not, GPS-off-by-default silently stops fees and commissions. ELD polling (Samsara/Motive, bl_int_0107) is a separate source and unaffected. |
| Privacy page ↔ portal behaviour | UNKNOWN | Not readable. | Same staging test as above. |
| W-9 (LB-W9-v2) | PARTIAL | Only `app/carrier/w9-form.js` changed. The **agent portal** has its own W-9 / W-8BEN substitute forms (bl_agent_0079–0082) — untouched. Existing rule from 5 Sep review: single-member LLC marked Partnership = reject. | Counsel/tax review still required; executed record must be immutable (PDF snapshot + hash), not re-rendered from the live template. |
| Signup clickwrap | PARTIAL (branch admits it) | Acceptance refs only in `auth.users.raw_user_meta_data` — user-editable via `updateUser`, not append-only. `handle_new_user` (bl_bp_0313) reads metadata; extra keys are harmless unless it validates strictly. | Add a server-side `policy_acceptances` table (user, org, policy_ref, sha256, ip, ua, ts) written by trigger on signup. |
| Partner rate-confirmation template change | UNKNOWN | Not readable. | Confirm signed RCs are stored as rendered snapshots; if the RC is re-rendered from the live template, every already-signed RC changes text retroactively. |
| SEO metadata (≤65 / ≤160, normalizer) | UNKNOWN / **SEO risk** | `public_truth_normalize()` + truncation run over ALL generated pages. R1–R7 title surgeries (`docs/seo/WEEKLY-LOG.md`) were deliberate; the homepage title is "Truck Dispatch & **Verified** Load Board for Carriers \| LoadBoot"; `/ghost-loads-load-board-problems` is a ranking article about ghost loads. | Before deploy: full before/after diff of every `<title>` and `<meta name=description>` (site/ vs current live). Any change to an R-round page must be logged in WEEKLY-LOG. Confirm the three stale-data guards in `market_reports_module.py` survived (engine correctly publishes nothing since week 34). |
| Sitemap | PASS (model level) | Dropping `changefreq`/`priority`/fabricated `lastmod` is harmless; `/app/` is already robots-disallowed. | Editorial `lastmod` still empty — fine for now. |
| Source claim cleanup | UNKNOWN | Not readable. Branch admits legacy claims remain in templates and are only masked by the normalizer. | Source-of-truth drift: fix templates, don't rely on output rewriting. |
| Automated tests / CI | UNKNOWN | Not readable. GitHub Actions does not gate Netlify (Netlify deploys `main` on push). | Enable branch protection requiring `quality.yml` on `main` or the tests never block a deploy. |
| Authenticated staging | FAIL (not done) | No staging run reported; project id `snslhvmkjusozgjelghi`. | Run: signup clickwrap → agreement v3 sign → book → manual check-in → delivered → fee invoice → agent accrual. |
| Production readiness | FAIL (not ready) | Not pushed, not deployed, DB-side contradictions above unresolved, PWA SW cache must be cleared after deploy. | Do not call it "production live and proven". |

## 2. Confirmed PASS (model level only)
- No allocation of traffic / carrier-appointed limited dispatch agency — consistent with the compliance guide and with "no broker MC".
- No freight-money custody — consistent with the manual pay-rail engine.
- FMCSA `unknown` ≠ "no authority" — matches the existing bl_fm_0226 rule.
- Sitemap cleanup, /app/ exclusion.

## 3. Partial improvements
- Clickwrap (metadata only), W-9 (carrier only), agent commission wording (1% vs up-to-2%), source claims masked not fixed.

## 4. FAIL / blockers
1. Fee timing: DB issues the fee invoice at delivery; the new legal text says after carrier payment. Resolve before merge.
2. No authenticated staging run of the GPS-off path through `delivered` → invoice.
3. No production proof; nothing pushed.

## 5. UNKNOWN — needs real code / staging / DB evidence
- Everything code-level (items 1–36 of the request) — branch must be pushed to `origin` (or copied into the local folder) so it can be read.
- `fin_invoices.gross` composition (linehaul vs linehaul+accessorials).
- Old-version agreement/RC snapshot behaviour.
- Title/meta before-after diff.

## 6. Handoff protocol (both assistants)
- The other assistant must `git push origin audit/carrier-legal-seo-hardening-v2` (no merge) so this side can read it; until then this side cannot go past model-level review.
- Every session appends its findings to this file (`docs/audit-2026-09/REVIEW-LEGAL-SEO-HARDENING-2026-09-05.md`) — do not overwrite earlier sections.
- Yaseen pushes; nobody deploys. Prod Supabase `rwscphuhpjoudvljvmdk` is never touched for tests; use `DO $$ … raise exception 'RESULT: %' $$` on staging.

---

# ROUND 2 — re-audit request (branch + `4adebcd` / `bl_pay_0120`) — Claude, 5 Sep 2026 (later)

## R2.0 What was and was not readable

| Source | Status | Detail |
|---|---|---|
| Branch `audit/carrier-legal-seo-hardening-v2` (`efc5f5d`, `6d50655`, `4adebcd`) | **NOT READABLE** | `git ls-remote https://github.com/yaseem786/loadboot` lists only `main` (b44fbab), `feat/dispatcher-model`, `fix/live-chat-*` (2), `preview/*` (2). Local repo `.git/refs/heads` has `main`, `feat/*` (3), `preview/command-center-v1`, `shots-3x-refresh`. No `migrations/bl_pay_0120*`, no `docs/OPERATING-MODEL.md`, no `.github/workflows/` anywhere. |
| `origin/main` @ `b44fbab` (2 commits after the branch base `44790d6`) | **READ IN FULL** | Cloned into the cloud container; `python3 build_site.py` run → `/site` generated; every file below was grepped/read directly. |
| Staging DB `snslhvmkjusozgjelghi` | **READ (SQL, read-only)** | Live function bodies, triggers, tables, seeds. |
| Production DB `rwscphuhpjoudvljvmdk` | **READ (md5 + counts only)** | Parity check against staging. No writes. |
| Yaseen's local shell (`device_bash`) | unavailable | "Workspace unavailable" — 5th time since 25 Aug. |

So: every claim below about *the branch's code* is UNKNOWN; every claim about *what the product does today* is PROVEN on main + both databases. The gap between the two is exactly what the branch must close.

## R2.1 Verdict table

| Area | Verdict | Exact evidence | Remaining risk | Required next action |
|---|---|---|---|---|
| **Fee timing** (rule 14) | **FAIL on main/DB; branch UNKNOWN** | Staging+prod `app_private.auto_invoice_on_delivery()` (identical md5 `f6d626f9…`): on `trips.status → 'delivered'` inserts `fin_invoices … status='sent', issued_at=now(), due_at=current_date+15`, in-app notice "5% service fee … due within 15 days", and `sys_email(... 'dispatch fee due (net 15)')`. Trigger `trg_auto_invoice` on `app_private.trips`. `pay_confirm_received` flips `platform_fee` transfers → invoice `paid` (carrier pays LoadBoot; no link to freight payment). `bl_pay_0120` is **not applied** on staging or prod (`schema_migrations` has no `pay_0120`/`fee_after` row) and not in the repo. | Until 0120 is read and applied on staging, delivery still emails a net-15 fee invoice. Also: `fire_referral_accrue` fires on `status in ('sent','paid')` → if 0120 leaves the invoice in `draft` until freight is paid, commissions correctly wait; if 0120 invents a new status value, the CHECK `status in ('draft','sent','paid','void')` will reject it. | Push branch; apply 0120 to **staging only**; test: deliver → row `draft` + no email → `pay_confirm_received(freight)` → `sent`/due → commission row appears. Verify already-`sent` rows (13 on staging, 1 paid on prod) are left untouched or migrated deliberately. |
| **Fee base** (rules 13, 15) | **PARTIAL → commission leak FAIL** | Fee = `round(rate*0.05,2)` on `trips.rate` (linehaul) — OK. But `migrations/wave-accessorial-payout/wa_0001_accessorial_to_invoice.sql` (applied on prod: `apply_accessorial_to_invoice` exists) does `update fin_invoices set gross = gross + a.amount, net = net + a.amount` for every approved accessorial. `bl_agent_0306` commission = `round(gross*pct/100,2)` on `fin_invoices.gross` → **agents earn on detention/TONU/lumper**. Fee column stays linehaul-only, but `fee_pct`=5 no longer equals `fee/gross`; any PDF/UI that recomputes % from gross is wrong. | Rule 15 is violated on the commission side and the invoice `gross` no longer means "gross linehaul". | **New migration needed**: add `fin_invoices.linehaul_gross` (or `accessorial_total`), stop adding accessorials into `gross` (put them in a separate column/adjustment only), and change `referral_accrue_core` to use linehaul base. Backfill from `fin_adjustments(kind='accessorial')`. |
| **GPS / manual delivery** (rules 17, 18) | **FAIL on main/DB; branch UNKNOWN** | (a) `app/carrier/app.js:4988` `ensureLiveLoc()` — comment "MANDATORY tracking: auto-starts with the trip"; calls `pocketSetConsent(tripId,true)` **silently**, toast "Live tracking started automatically… required by the rate confirmation"; invoked at portal boot (`:5010-5014` "start on ANY portal entry"), dashboard render (`:3167`) and trips view (`:4420`). (b) Manual path exists in the API (`tripSetTracking → cc_trip_set_tracking('manual_checkin')`, `tripCheckin → cc_trip_checkin`) but is **called 0 times** in `app/carrier/app.js`. (c) DB: `cc_pocket_advance_trip(p_trip,'delivered')` raises `'Check in at the delivery first — delivery must be GPS-verified'` unless a `trip_dwell_events` row with `lat is not null` exists (unconditional check, staging = prod md5 `4c4ff498…`); `cc_trip_depart` raises `'GPS required'` when stop coords exist. Only staff `cc_advance_trip` can mark delivered without GPS. | A GPS-off carrier **cannot reach `delivered`** → no invoice → no fee → no commission. The branch's frontend change alone (no GPS auto-start) would make the chain silently stop unless the DB gate is changed. | Migration required: a manual-checkin branch in `cc_pocket_advance_trip` (accept `trip_dwell_events` rows with `source='manual_checkin'` + POD upload as delivery proof), same for `cc_trip_depart`; wire `tripSetTracking`/`tripCheckin` into the carrier UI; keep `cc_pocket_set_consent(false)` as revoke. Then run the full staging path. |
| **Agent commission** (rule 16) | **PASS (engine) / PARTIAL (copy)** | `app_private.referral_levels` = `[1.00, 0.50, 0.25, 0.15, 0.10]` on **both** staging and prod; `referral_accrue_core` md5 identical on both. | Copy check on the branch not possible; on main the agent portal (`app.js:~1143`) describes levels, but public/legal docs written for the branch say "1% of the 5%". | Branch docs must state L1 1% + overrides = up to 2% of gross, and that the base is gross linehaul (after the fee-base fix). |
| **Dispatch agreement** | **FAIL on main; branch UNKNOWN** | `app/carrier/dispatch-agreement.js` main: §2 "Nothing here creates … agency" vs §1/§5 "on Carrier's behalf … book loads"; §5 "**auto-booked** within Carrier's pre-approved limits"; §7 "collected by **settlement deduction** or invoice"; §13 **180-day** non-circumvention; `REF='LB-DSA'` (no version). Storage `app_private.dispatch_agreement_signatures(carrier_id, signer_name, signed_date, agreement_ref, signed_at)` — **no body snapshot, no hash, no IP/UA**. `master_agreements(kind,version,body_md…)` + `org_agreement_acceptances` exist but the carrier flow writes `dispatch_agreement_signatures`. | Even if v3 text is right, the executed record is a name + timestamp against a mutable JS constant. | Store `agreement_ref` + `sha256(body)` + full rendered body in `dispatch_agreement_signatures` (or move to `master_agreements`/`org_agreement_acceptances` with `version`), add ip/user_agent columns; re-acceptance prompt for carriers signed under the old text. |
| **Payment custody** (rules 11, 12) | **PARTIAL** | Manual rails (`pay_transfers`, `pay_instructions`, factoring/NOA) = broker pays carrier/factor directly — PASS. BUT `public.cc_create_settlement` / `cc_decide_settlement('pay')` (wave5) still exist on staging: they bundle carrier invoices into `fin_settlements(gross,fee,net)` and "PAY OUT" net — a LoadBoot-pays-carrier model. Marketing nav on all 121 pages links `payments-settlements.html`; carrier copy "settlement itemized". | Dead-but-live functions and "settlement" wording contradict rule 12. | Decide: drop/disable settlement RPCs (revoke execute, remove CC view) and rename `payments-settlements` page/nav to "Payments"; or document them as internal fee bundling only (then rename `net`). |
| **W-9 / W-8 — carrier** | **PARTIAL** | `app/carrier/w9-form.js`: Part II certification text present (`:43`), "Signature of U.S. person" (`:44`), two certify checkboxes (`:268-275`), saved via `cc_carrier_submit_w9` into `app_private.w9_submissions(name, business_name, classification, llc_class, address, city_state_zip, tin, tin_last4, signer_name, signed_date, signed_at, ref)`. **No** backup-withholding-exempt box, **no** waiting-for-TIN, **no** W-8 route, **no** rendered snapshot/hash/IP. Full `tin` stored in clear text in a normal column. | The branch's `LB-W9-v2` text change does not fix storage. Storing plaintext TIN is a data-protection risk regardless of wording. | Add `form_version`, `body_hash`, `snapshot_html`, `ip`, `user_agent`, `exempt_backup_withholding bool`, `awaiting_tin bool`; encrypt or vault the TIN (pgsodium / edge fn), keep `tin_last4` only in the row; counsel review of the substitute form. |
| **W-9 / W-8 — agent** | **PARTIAL** | `app/carrier/app.js:723` `openPrintable('Form W-9 (Substitute)' / 'W-8BEN (Substitute)')`, `:1269` tax-form select w9/w8ben, `:1313-1328` "your e-signature below also signs this W-9/W-8BEN". Stored inside `agent_profiles.payout_details` JSON (per bl_agent_0079-0082). Not touched by the branch. | Same gaps as carrier plus JSON-blob storage. | Same fix set; one shared `tax_forms` table for both roles. |
| **Signup clickwrap** | **FAIL on main; branch PARTIAL by its own admission** | Main: `app/carrier/app.js:506` and `app/partner/app.js:551` call `signUp(em,pw,{company,name,phone|partner_kind})` — **no Terms/Privacy checkbox, no accepted_* metadata**. Staging/prod `public.handle_new_user()` reads only `company,name,role,partner_kind`. **No** `policy_acceptances`/`terms_acceptances` table exists (searched `information_schema` for accept|consent|policy|tos: only `org_agreement_acceptances` for master agreements). | Branch stores refs in `raw_user_meta_data` — user-editable via `auth.updateUser`, not append-only, no hash/IP/UA. | Migration: `app_private.policy_acceptances(id, user_id, org_id, policy_key, policy_version, policy_sha256, accepted_at, ip, user_agent, source)`, insert-only RLS, written by `handle_new_user` from metadata **and** by an RPC on later re-acceptance; expose a signed-PDF/HTML download of the accepted version. |
| **Rate confirmation ↔ Terms consistency** | **UNKNOWN (branch) / main FAIL** | Main carrier toast says tracking is "required by the rate confirmation"; DB `build_rate_confirmation` trigger on `trips` renders the RC at booking (`trips_rate_confirmation → app_private.build_rate_confirmation`). | Whether signed RCs are stored snapshots or re-rendered from the live template was not verified this round. | Read `build_rate_confirmation` + `load_rate_agreements` columns; confirm snapshot. |
| **Source-level claims** | **FAIL on main; branch UNKNOWN** | Source files (excluding site/docs): "zero ghost" in `build_site.py`, `about_module.py`, `app/shared/ui/lcOnboard.js`, `app/carrier/app.js` (5 files); "every load is real" 2; "auto-filed" 5; "auto-paid" 4; "claims actually get paid" 1; "GPS on every" 1; "court-grade" 2; "zero double-broker" 2; "loads from day one" 2; "30–50%" 2; "we match" 2. Generated: the **site-wide default meta description** on 121/124 pages = "…verified load board with **zero ghost loads** … **live GPS tracking**" (`site/index.html` and all pages sharing the default). "settlement" appears on 121 pages via the nav link `payments-settlements.html`. | The branch's normalizer masks output; the sources above remain the truth in the repo, and the default meta description is the single highest-exposure claim. | Fix the default description in `build_site.py` (one line), then the 5 source files; keep the normalizer only as a regression test, not as the fix. |
| **SEO metadata (main baseline)** | **FAIL on main (as expected); branch UNKNOWN** | Generated main: 124 pages; **39 titles > 65** (e.g. `cost-per-mile-calculator.html` 79, `create-agent-account.html` 78); **44 descriptions > 160** (e.g. `cost-per-mile-calculator` 243, `box-truck-freight-rates` 224); 3 missing description; 2 missing canonical; 0 exact-duplicate titles; 0 exact-duplicate descriptions (the R-round "identical rate-hub meta" is near-duplicate, not exact). Full table: `lb_meta_main.tsv` (in this session). | If the branch truncates 39 titles mechanically, R1–R7 surgeries change silently. | Produce the before/after diff (branch build vs this baseline); every changed title on an R-round page goes into `docs/seo/WEEKLY-LOG.md`. |
| **Sitemap** | **PARTIAL on main; branch claims PASS (UNKNOWN)** | `site/sitemap.xml`: 117 `<loc>`, 117 `<changefreq>weekly` (from `build_site.py:8389`), 0 `<priority>`, 0 `<lastmod>`, 0 `/app/` or `dashboard` URLs (robots.txt already `Disallow: /app/`, `/dashboard.html`). | Only `changefreq` is the defect; nothing private leaks today. | Drop `changefreq`; add real `lastmod` from `article_dates.py` (git first-commit dates exist — the mechanism is already in the repo). |
| **Article dates / E-E-A-T** | PARTIAL | `article_dates.py` derives real first-commit dates from git → `article_dates.json`; datelines are real where used. Author/reviewer/methodology blocks not audited page-by-page this round. | Unknown coverage. | Count pages with author + methodology blocks in the branch build. |
| **CI / tests / release gate** | **FAIL on main; branch UNKNOWN** | No `.github/` on main. `tests/` = `ai_research_footer_checks.py`, two `.mjs` regression tests, `security/`. `netlify.toml`: `command = "python3 build_site.py"`, `publish = "site"` — **Netlify deploys every push to main with no test gate**; no branch protection evidence. | Even with the branch's workflow, a green CI does not block Netlify. | Enable GitHub branch protection (required check `quality`), or move the tests into the Netlify build command so a failing test fails the deploy. |
| **Authenticated staging** | **FAIL (not done)** | No `bl_pay_0120` on staging; no test run recorded. | — | Run the 9-step path below on staging under a `DO $$ … raise exception 'RESULT: %' $$` block for the DB half and a real signup for the UI half. |
| **Production readiness** | **FAIL** | Branch unpushed; 0120 unapplied; GPS gate and fee-base leak need new migrations that do not exist yet; no acceptance ledger; no CI gate on deploy. | — | Not "production live and proven". |

## R2.2 Confirmed PASS (proven on main + DB)
- `referral_levels` = 1.00 / 0.50 / 0.25 / 0.15 / 0.10 on both DBs; engine md5 identical staging = prod.
- Fee column = 5% of `trips.rate` (linehaul) — the *fee* itself does not include accessorials.
- Manual pay rails: broker/shipper pays carrier or factor directly; `pay_confirm_received` only marks LoadBoot's own fee invoice paid.
- Manual-check-in RPCs (`cc_trip_set_tracking('manual_checkin')`, `cc_trip_checkin`) exist server-side with `location_consent=false` semantics; `cc_pocket_set_consent(false)` is a working revoke.
- Sitemap: no private URLs, no `priority`, no fabricated `lastmod`; robots disallows `/app/`.
- Real article dates mechanism exists (`article_dates.py`).

## R2.3 Partial improvements
- Fee base (fee OK, commission base wrong), W-9 carrier (text OK, storage/routing missing), W-9 agent (untouched), custody (rails OK, settlement RPCs + wording remain), sitemap (`changefreq` only), signup clickwrap (metadata-only by the branch's own description).

## R2.4 FAIL / blockers
1. `auto_invoice_on_delivery` still issues `sent` + emails at delivery on **both** DBs; `bl_pay_0120` unapplied, unreadable.
2. `cc_pocket_advance_trip` requires a GPS-verified delivery row — GPS-off carriers cannot deliver; carrier UI auto-starts GPS and silently sets consent true; manual RPCs unused in UI.
3. `wa_0001` adds accessorials into `fin_invoices.gross` → referral commissions on accessorials (needs a migration).
4. No server-side acceptance ledger; no hash/snapshot for agreement or W-9; plaintext TIN in `w9_submissions.tin`.
5. Site-wide default meta description carries "zero ghost loads … live GPS tracking" on 121 pages; 5 source files still contain the claims.
6. No CI on main and Netlify is not gated by any test.
7. Branch not on `origin` → nothing in it is verifiable.

## R2.5 UNKNOWN — needs the branch / staging / production
- Everything inside `efc5f5d`, `6d50655`, `4adebcd` (including what `bl_pay_0120` actually does and whether it touches `fire_referral_accrue`, already-sent rows, or the CHECK constraint).
- Whether signed rate confirmations are snapshots (`build_rate_confirmation` / `load_rate_agreements`).
- Title/meta before-after diff of the branch build vs the main baseline (`lb_meta_main.tsv`).
- Author/reviewer/methodology coverage per page; GSC per-query rows for the 28 new pages (still zero impressions as of R7) — page merge/delete decisions cannot be made yet.
- Deploy fingerprint, rollback rehearsal, PWA SW cache clear.

## R2.6 Staging test path (must all pass before "ready")
1. `git push origin audit/carrier-legal-seo-hardening-v2` (no merge). 2. Apply `bl_pay_0120` + the two new migrations (GPS manual gate, fee-base split) to **staging only**. 3. Fresh carrier signup with clickwrap → row in `policy_acceptances`. 4. Sign agreement v3 → `dispatch_agreement_signatures` row with hash. 5. Book a load, choose `manual_checkin`, never grant geolocation. 6. Manual check-in at pickup/delivery + POD → `cc_pocket_advance_trip('delivered')` succeeds. 7. `fin_invoices` row = `draft`, **no** `fee.invoice_due` email in `email_queue`. 8. Broker `pay_trip_mark_sent` → carrier `pay_confirm_received(freight)` → invoice `sent`, due date set, email now. 9. Approve a detention accessorial → carrier payable +amount, `fee` unchanged, commission unchanged (linehaul base). 10. `referral_commissions` row = 1% of linehaul for L1. 11. Rollback: revert the three migrations on staging and re-run 6–8.
