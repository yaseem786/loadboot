# AUDIT HANDOFF — the shared baton between Claude and ChatGPT

**Rule:** Whoever gets "continue" from Yaseen reads THIS file first (after the re-sync header), works the `NEXT ACTION`, then rewrites the `CURRENT STATE` block and appends one `LOG` line before stopping. Never work from memory of an earlier turn — the other assistant may have moved things since. If this file and the repo/DB disagree, the repo/DB wins; fix the file.

Both assistants follow the same guardrails (`docs/CHATGPT-AUDIT-PROMPT.md`), the same sprint plan (`90-DAY-PLAN.md`), the same findings (`PHASE1-AUDIT.md`), and the same re-sync hard rule (`REPLY-TO-CHATGPT-phase1-review.md` → "HARD RULE"). Staging first; prod only on Yaseen's explicit "apply to prod" / "deploy"; Yaseen sends all personal carrier/broker messages.

**Standing content rule (Yaseen, 5 Sep):** every SEO/content decision must start from LIVE Google Search Console query/page data and GA4 — never from guessed keywords. Recipe: `seo-pull` edge fn via `net.http_post` → poll `net._http_response`, `dimensions:['page'|'query'], rowLimit:500` (see project memory `seo_measurement.md`). Applies to the 90-day SEO calendar in `90-DAY-PLAN.md` — re-rank it against real queries before writing anything.

---

## CURRENT STATE  (rewrite this whole block every turn)

- **Updated:** 2026-09-06 by Codex. Verification findings are in PHASE1-AUDIT.md, “2026-09-06 — Sprint 1+2 verification.” This supersedes the historical blanket CLOSED status; deployed work was not rebuilt.
- **Latest main:** 25e6e5b8ab5b4f97206953dcea061e4e4c071e83, re-fetched this turn. **Audit source push:** feat/dispatcher-model at 2d0a1b69e7944153388402129af3c1de09f11ec0 is present but unmerged. The review branch audit/s1-s2-verification-20260906 starts there to preserve its newer docs. Its verification commit changes documentation only.
- **Prod checkpoint:** 724 migrations; newest 20260906071245 bl_disp_0304_dispatcher_id_verify (other lane), then bl_bp_0321_agent_confirm_resend_idem > bl_sec_0320_email_ingest_service_only > bl_cmp_0325b > bl_cmp_0325 > bl_cmp_0324 > bl_fin_0322 > bl_bp_0323 (other lane). All six audit migrations are present.
- **Staging checkpoint:** 698 migrations; newest 20260906071229 bl_disp_0304_dispatcher_id_verify (other lane), then bl_avail_0321 (other lane) > bl_cmp_0325b > bl_cmp_0325 > bl_cmp_0324 > bl_fin_0322 > bl_avail_0320 (other lane) > bl_bp_0323 (other lane) > bl_bp_0321b. Earlier bl_sec_0320 is present.
- **Deployed edge sources:** load-mail v8 (prod slot 6 / staging 8), domain-check source v2 (prod slot 2 / staging 3), doc-precheck source v5 (prod slot 4 / staging 5). All three verify_jwt=true. Retrieved prod/staging sources match each other and the pushed files; doc-precheck slot-counter UNKNOWN is resolved.
- **F32 PASS:** exact existing rollback test staging 2850→2960, prod 1450→1560; six RPCs and grants verified; finance rows/prefs restored. UI smoke remains UNKNOWN.
- **F05 PASS:** exact existing rollback test both environments: 2 deliveries, 2 codes, 2 distinct keys, exactly 1 live. The three intended substitutions are the only differences from the saved old prod body. No email left the transaction.
- **F01 PARTIAL / OPEN:** SQL guard-only comparison exact against all three old prod bodies; existing SQL test PASS staging and prod, LB403 for refused callers, 0 refused writes, service_role reaches body. Current load-mail v8 still promotes caller-controlled input to service-role RPCs without checking an authorized ingestion sender in the handler; reproduced offline against deployed source with all fetches mocked.
- **F02 OPEN:** source/JWT settings verified, but current source admits private IPv6 redirects (including bracketed loopback) in the existing isolated reproduction. No private destination contacted. No F02 test file exists in the pushed tests directory; this turn did not re-run the owner-reported live HTTP 200/401/IPv4 probes.
- **F30 OPEN — broker regression:** current collector gives authorityVerified precedence before checking org kind. Conflicting response fields can flip the old broker active/inactive result in either direction; read-only counterexamples confirmed. Broker lapse side-effect block is still present, but its triggering predicate is changed. Old body was verified against historical migration plus saved pre-0324 hash.
- **F30 carrier coverage:** existing test BLOCKED on staging before writes (no unchecked carrier fixture), PASS on prod (one carrier collected, no owner email/pause, one pending_review row, dedupe and ACL assertions hold). No test was rewritten. Snapshot around 07:30 UTC: 44 non-demo/non-archived carrier check rows: 18 active, 3 inactive, 1 error, 22 no_docket; 21 auto pending_review verifications; 21 fmcsa safety rows plus 1 manual. These replace the earlier partial status breakdown for this snapshot.
- **Broker evidence:** backfill code selects carriers only; current broker checked_at rows show zero in the backfill-to-06:10 window, but these are not immutable history. Daily dispatcher 06:10 and observed collector runs through 07:30 succeeded. The two no_docket broker outcomes do not exercise the changed response classifier. At the later read M Usman Farooq (Agent) was pending with an mc_authority expiry at 07:30:00.355233 UTC; Vertex remained active. No inspected authlapse email/lapse notification for those brokers. Cause of the later pending/expiry state is UNKNOWN; do not attribute it to backfill or this test.
- **F31 PARTIAL:** existing SQL test PASS both envs; write-once RPC and stamp verified; documents anon grants 0; cc_list_documents EXECUTE exactly postgres/authenticated/service_role and ai_verdict return preserved. Synthetic mocked PDF tests: pass→warning, warning→warning, reject→reject, queued→queued. No forensic-only reject.
- **F31 source gap:** CC display, migrations, edge source and SQL tests are on 2d0a1b6. app/shared/api.js and the four app/carrier/app.js call sites still do not forward/persist the precheck verdict on either inspected ref. Do not recreate these missing edits; Yaseen will supply the actual changes. Production frontend deploy revision / upload-to-reviewer smoke is UNKNOWN.
- **Rollback status — blocked for failed paths; none executed persistently:** bl_cmp_0324_rollback explicitly leaves collect/dispatch changed, so cannot restore the broker classifier; bl_sec_0320_rollback does not fix the edge boundary; F02 has no matching SQL helper; F31's passing SQL cannot supply missing client code. No silent corrective SQL/edge deploy, real backfill, customer message or other-lane change occurred.
- **Three schema traps retained:** carrier_safety source must be manual|fmcsa (and safety rating clamped); org_docket needs owner-profile fallback and no_docket must be retriable; F05 throttle is agent_parents.sent_at, code link parent_id, emails contact_email/fmcsa_email.
- **F33 remains no-docket carriers, design only.** Historical count 27/44 preceded the later backfill; today's scoped check-row snapshot shows 22 no_docket. Do not rename/reuse F33 for an unrelated finding or start implementation. WhatsApp toggle also awaits Yaseen's go-ahead.
- **SEO untouched this turn:** live GSC baseline supplied/pulled 5 Sep = 60 clicks / 5,224 impressions / position 35.8, 454 named non-brand queries with zero clicks. Preserve the re-ranked calendar; week 1 copy/build was not started while verification findings remain for review. LOADBOOT_STAGING_ANON_KEY is mandatory for a staging-bound build.
- **Other outstanding owner actions:** review the findings/rollback scope; confirm/push F31 carrier upload edits; UI smoke Earnings and CC Documents; move the parked CI workflow when authorized. No dead-edge deletion or other-lane cleanup performed.

## NEXT ACTION  (one concrete step — the thing "continue" means)

**Review and resolve the verification blockers with Yaseen before implementation:** F30's broker classifier and incomplete rollback, F01's load-mail sender boundary, F02's IPv6 guard, and the missing F31 carrier upload edits. Do not substitute the passing narrow SQL tests for full closure. The requested helper-based rollback could not restore these failing paths; the specific reasons are recorded above and in PHASE1-AUDIT.md.

On the next prompt, re-sync latest main, the pushed audit branch, both migration lists and both edge inventories first. Preserve dispatcher/availability/0323 work. Any approved corrective work must start on staging with rollback-transaction verification before an explicitly authorized prod step.

After this review gate is cleared, continue the existing week-1 SEO scope: market-rates description/hub links/answer-first FTL section without retitling; TONU definition opener; ghost-loads snippet. Build with LOADBOOT_STAGING_ANON_KEY. F33 and WhatsApp remain design-only until Yaseen says go.

## LOG  (append one line per turn; newest last)

- 2026-09-05 05:xx UTC · ChatGPT · Phase 1 audit delivered (PHASE1-AUDIT / COMPETITION-AND-WEDGE / 90-DAY-PLAN / EVIDENCE-INDEX). No code changed.
- 2026-09-05 06:2x UTC · Claude · Verified F01 + F02 on prod/repo (both real). Wrote REPLY-TO-CHATGPT with corrections, Q1–Q10 answers with `[YASEEN]` blanks, scoped GO for Sprint 1, re-sync HARD RULE, relay protocol. Created this file. No code changed.
- 2026-09-05 07:30 UTC · Claude · SPRINT 1 BUILT ON STAGING (Yaseen said "start"; ChatGPT out of quota). F01: caller inventory (only load-mail), `bl_sec_0320` applied staging + rollback-txn test PASS, load-mail v8 deployed staging (Bearer SVC). F02: domain-check v2→v3 deployed staging with verify_jwt=true; exact-key auth broke the real caller in test (fmcsa_config anon key ≠ env ANON_KEY) → switched to verified-claims check (role+ref); SSRF guards + unit tests. F05: `bl_bp_0321` — first apply used uuid, test threw 22P02 (verify_codes.id is bigint), rolled back via helper, re-applied as 0321b; test PASS (2 sends → 2 keys). F07: patcher + applied in place to disk build_site.py (mtime-guarded, checksum-verified); 3 build modes verified. F18: check_imports fix, package.json scripts, CI yml (protected path → parked in docs/). Files on disk, uncommitted. Prod untouched.
- 2026-09-05 08:00 UTC · Claude · "continue" (ChatGPT still out). Re-sync: main b44fbab unchanged, prod 0319 unchanged, staging 0320/0321/0321b. Sprint 2 prep READ-ONLY: F30 auto-FMCSA-at-signup is a no-op (no auth/carrier_org → fmcsa-verify never persists; 22 queued / 1 row in 30d), F31 ai_verdict never written (58/58 NULL; doc-precheck advisory-only, source not in repo), F32 RPC drift root cause = staging-only wd_0032_per_trip_pnl_engine + wd_0033_carrier_cost_model_rpc (11 Jul), prod reused those numbers for other migrations. Written to PHASE1-AUDIT.md changelog. No code, no DB changes.
- 2026-09-05 20:50 UTC · Claude · ChatGPT re-synced and correctly BLOCKED: HANDOFF + Sprint 1 files exist only on Yaseen's disk (no git from Claude's side); staging shows `bl_bp_0323_submit_load_kind_aware` from another lane (20:41 UTC, no file on disk, not on prod) — recorded, not touched. Wrote `PUSH-SPRINT1.md` (exact git commands for Yaseen). No code/DB changes.
- 2026-09-05 21:45 UTC · Claude · "continue" (ChatGPT hit its limit mid-turn; its F32 output never reached disk/GitHub — redone). Re-sync: main d84e6d2 (PR #169 merged Sprint 1), prod newest bl_bp_0323 (other lane), staging bl_avail_0320 (other lane). F32: wrote `migrations/bl_fin_0322_port_trip_pnl_engine.sql` (wd_0032+wd_0033 verbatim + explicit grants + ACL check), prod pre-flight clean (only the 4 new prefs columns missing), applied on STAGING (idempotent), rollback-txn test `tests/bl_fin_0322_rollback_test.sql` → RESULT PASS (net 2850→2960, 42501 + ACL guards hold, 0 rows left). Prod untouched. Files on disk, uncommitted.
- 2026-09-05 22:00 UTC · Claude · Yaseen: "fix kary usy" (prod Earnings error) → `bl_fin_0322_port_trip_pnl_engine` APPLIED TO PROD after re-checking prod state matched pre-flight; prod rollback test RESULT PASS (trip 55a9c732… net 1450→1560, 0 rows left); post-check 6 RPCs, ACL authenticated+service_role only. First audit migration on prod. Sprint 1 items still staging-only.
- 2026-09-05 22:40 UTC · Claude · "continue". Re-sync: both DBs newest bl_fin_0322; edge fns unchanged; Yaseen pushed F32 files to main. F30 + F31 DESIGN NOTES written to PHASE1-AUDIT.md (text only; prod evidence: 44 carriers / 0 authority checks / dispatch gated on an onboarding item carriers never get; documents.ai_verdict never sent by the client insert). Live GSC pull (4 calls): 60/5,224/35.8; 90-DAY-PLAN SEO calendar re-ranked with a per-cluster table. Housekeeping: prod `lb-tmp-keyread` = dead 410 stub. No code, no DB changes this turn.
- 2026-09-05 23:00 UTC · Claude · Yaseen: "go and complete all". SPRINT 1 + SPRINT 2 ITEMS 1–3 ALL APPLIED TO PROD, each staging-first with a rollback-txn test re-run on prod: F30 bl_cmp_0324 (+ real backfill: 22 carriers checked, 21 pending_review verifications — was 0 of 44), F31 bl_cmp_0325 + 0325b + client/CC/doc-precheck v5 with PDF forensics, F01 load-mail v8 then bl_sec_0320 (LB403 guard), F02 domain-check v2 verify_jwt=true (forged JWT 401, metadata IP refused), F05 bl_bp_0321 (2 distinct keys). Three schema traps caught by the tests before prod (carrier_safety source CHECK, docket living on the owner's profile, sent_at throttle). New finding F33: 27 of 44 carriers have no docket anywhere. Files on disk, uncommitted.

- **2026-09-06 — Codex verification checkpoint (in progress):** main 25e6e5b; later audit sources located on feat/dispatcher-model at 2d0a1b6. F32 rollback tests PASS staging/prod (2850→2960 / 1450→1560). F01 SQL guard-only comparison passed; deployed edge caller boundary still fails a mocked reproduction. F02 deployed IPv6 redirect checks still fail a mocked reproduction. F30 broker classification differs from pre-migration prod; its rollback helper explicitly retains the changed collector and cannot restore this path. No persistent database changes or messages sent. Existing newly located SQL tests and final report pending.

- **2026-09-06 · Codex · Sprint 1+2 verification completed with findings.** Read latest main 25e6e5b and pushed audit sources 2d0a1b6; both corrected DB checkpoints match. Existing F32/F31/F01/F05 SQL rollback tests PASS staging→prod; F30 staging fixture exhausted (no writes), prod test PASS. Reopened F30 for changed broker classifier/incomplete rollback; F01 for edge caller promotion; F02 for IPv6 redirect bypass. F31 DB/forensics PASS but claimed carrier upload wiring missing from both inspected refs. Detailed dated PHASE1 section, CURRENT STATE and NEXT ACTION updated; original logs retained. No persistent DB rollback/remediation, deploy, personal message, SEO, F33 or WhatsApp implementation. Correct rollback scope and missing source need owner review.
