# AUDIT HANDOFF — the shared baton between Claude and ChatGPT

**Rule:** Whoever gets "continue" from Yaseen reads THIS file first (after the re-sync header), works the `NEXT ACTION`, then rewrites the `CURRENT STATE` block and appends one `LOG` line before stopping. Never work from memory of an earlier turn — the other assistant may have moved things since. If this file and the repo/DB disagree, the repo/DB wins; fix the file.

Both assistants follow the same guardrails (`docs/CHATGPT-AUDIT-PROMPT.md`), the same sprint plan (`90-DAY-PLAN.md`), the same findings (`PHASE1-AUDIT.md`), and the same re-sync hard rule (`REPLY-TO-CHATGPT-phase1-review.md` → "HARD RULE"). Staging first; prod only on Yaseen's explicit "apply to prod" / "deploy"; Yaseen sends all personal carrier/broker messages.

**Standing content rule (Yaseen, 5 Sep):** every SEO/content decision must start from LIVE Google Search Console query/page data and GA4 — never from guessed keywords. Recipe: `seo-pull` edge fn via `net.http_post` → poll `net._http_response`, `dimensions:['page'|'query'], rowLimit:500` (see project memory `seo_measurement.md`). Applies to the 90-day SEO calendar in `90-DAY-PLAN.md` — re-rank it against real queries before writing anything.

---

## CURRENT STATE  (rewrite this whole block every turn)

- **Updated:** 2026-09-05 22:00 UTC by Claude
- **Sprint:** 1 — boundaries: ALL 5 ITEMS built + verified on STAGING, files now ON `main` (PR #169). **Nothing on prod yet — waiting for Yaseen's "apply to prod" per item.** Sprint 2 item 1 (F32 port) — **DONE ON PROD** (Yaseen: "fix kary usy" 21:5x UTC): applied + rollback test PASS on prod. **First audit migration on prod.**
- **Working branch:** Sprint 1 files were merged to `main` by Yaseen via **PR #169 → `d84e6d2`** (no `audit/s1-boundaries` branch exists — do not look for it). This turn's F32 files are again written straight to Yaseen's DISK, UNCOMMITTED (Claude still has no git; device shell down) — Yaseen commits them: `migrations/bl_fin_0322_port_trip_pnl_engine.sql`, `docs/audit-2026-09/tests/bl_fin_0322_rollback_test.sql`, this file, `PHASE1-AUDIT.md`.
- **main at start of turn:** `d84e6d2` (PR #169). Other-lane files seen on disk since: `migrations/bl_avail_0320_daily_truck_availability.sql`, `migrations/bl_bp_0323_submit_load_kind_aware.sql`, `docs/audit-2026-09/REVIEW-LEGAL-SEO-HARDENING-2026-09-05.md` — not audit work, not touched.
- **ChatGPT's last turn (21:0x UTC) was LOST:** it finished "Prepare F32 migration, schema comparison, unrun rollback test" and hit its limit mid "update docs / run checks / commit and push" — nothing reached GitHub or disk. Claude redid F32 from scratch; ChatGPT must NOT redo it either.
- **Migrations high-water mark:**
  - prod: **`bl_fin_0322_port_trip_pnl_engine` (21:5x UTC, this turn — first audit migration on prod)** · `bl_bp_0323_submit_load_kind_aware` (20:42, another lane) · `bl_bp_0319`. Sprint 1 migrations (0320/0321) still NOT on prod.
  - staging (newest first): **`bl_fin_0322_port_trip_pnl_engine` (21:3x UTC, this turn — idempotent re-apply over the existing wd_0032/wd_0033 objects; only tightened grants)** · `bl_avail_0320_daily_truck_availability` (21:18, another lane) · `bl_bp_0323_submit_load_kind_aware` (20:41, another lane) · `bl_bp_0321b_agent_confirm_resend_idem_bigint` (live) · `bl_bp_0321_…` (superseded, rolled back) · `bl_sec_0320_email_ingest_service_only`.
- **Edge fns:** staging `load-mail` v8, `domain-check` v3 (verify_jwt=true) · prod `load-mail` "v7" source, `domain-check` v1 (verify_jwt=false) — unchanged.
- **Sprint 1 items (all staging-verified; prod step needs Yaseen's word):**
  | Item | Status | Prod step |
  |---|---|---|
  | F01 `bl_sec_0320` | staging ✓ | 1) deploy `load-mail` v8 to prod FIRST 2) `apply_migration` from `migrations/bl_sec_0320_…sql` 3) re-run rollback test on prod |
  | F02 `domain-check` v2/v3 | staging ✓ | deploy `supabase/functions/domain-check/index.ts` to prod with **verify_jwt=true**; run one `shipper_business_start` to confirm collector reads it |
  | F05 `bl_bp_0321` | staging ✓ (as 0321b) | `apply_migration` from `migrations/bl_bp_0321_…sql` (already bigint) |
  | F07 build default | on main | none (local behaviour). Yaseen: set `LOADBOOT_STAGING_ANON_KEY`, or `LOADBOOT_CONTEXT=production` for old behaviour |
  | F18 CI | on main | Yaseen moves `docs/audit-2026-09/pr-checks.yml` → `.github/workflows/`; optional secret `LOADBOOT_STAGING_ANON_KEY` |
- **Sprint 2 item 1 — F32 port (`bl_fin_0322_port_trip_pnl_engine`):**
  - File = the two staging statements `wd_0032_per_trip_pnl_engine` + `wd_0033_carrier_cost_model_rpc` verbatim (from staging `supabase_migrations.schema_migrations`) + explicit grants (revoke public/anon, grant authenticated + service_role) + ACL re-check. Header carries WHY / PRE-FLIGHT / TEST / ROLLBACK.
  - Prod pre-flight (read-only, done): every referenced column exists on prod (`trips.miles/rate/started_at/delivered_at/load_id/carrier_id`, `loads.miles/deadhead/rate/origin/destination`, `trip_accessorials.amount/status/billable/kind`, `carrier_dispatch_prefs.truck_mpg/fuel_price/cost_per_mile/updated_at`, PK(carrier_id), `my_carrier_org()`). Only the 4 new nullable prefs columns are missing — the migration adds them.
  - Applied on STAGING as `bl_fin_0322_port_trip_pnl_engine` → success, ACL notice ok, grantees = authenticated,postgres,service_role.
  - Rollback-txn test `docs/audit-2026-09/tests/bl_fin_0322_rollback_test.sql` run on staging → **RESULT PASS**: self-discovered trip `af8118e5…` net 2850→2960 (+150 earning −40 cost, factoring-aware), remove works, cost-model round-trip, `cc_carrier_earnings` keys `from,to,trips,totals`, bad direction/zero amount → 22023, non-carrier caller → 42501 on 3 RPCs, anon has no EXECUTE, 0 rows left after.
  - Test gotchas baked in: don't `SET ROLE authenticated` inside it (hides anon grants from information_schema → false PASS); expected net delta = 150·(1−factoring_pct/100) − 40, not a flat 110; `cc_set_cost_model` has 6 positional params — call with named args.
  - **PROD: DONE 21:5x UTC.** Pre-check: 0 RPCs / no table / 4 cols missing (as pre-flight). `apply_migration bl_fin_0322_port_trip_pnl_engine` → success. Same rollback test on prod → **RESULT PASS** (real trip `55a9c732…` net 1450→1560, remove ok, cost-model round-trip, 22023/42501 guards, anon no EXECUTE). Post-check: 6 RPCs present, `trip_finance_items` 0 rows, grantees authenticated+postgres+service_role, no prefs row touched. **Remaining: Yaseen smokes the carrier Earnings / trip P&L screen in the live carrier app once** (expects real numbers or 'set your cost model', not an RPC error).
- **Blocked on Yaseen:** "apply to prod" for Sprint 1 items (F01→F02→F05); smoke-test carrier Earnings screen on prod; moving the CI yml; committing this turn's files.
- **Do NOT touch (Sprint rule):** `app/carrier/app.js`, `app/shared/api.js`, outreach engine/schedule, auth flows, DNS/email identity, payments, agreements, CC nav. Other lanes' files above.
- **Open side-threads (not sprint work):** GABE LOGISTICS re-uploads pending (corrected W-9 + COI naming LoadBoot LLC, 30 N Gould St Ste N, Sheridan WY 82801 as holder) → approve, set COI `expiry_date=2026-12-01`, activate; fleet truck VIN 1M1AW21Y3GM077936 already added. Retell balance empty → CC contact-channel toggle (WhatsApp number instead of call button) is a backlog item, not started.

## NEXT ACTION  (one concrete step — the thing "continue" means)

1. Re-sync header (main sha vs `d84e6d2` + new commits, `list_migrations` prod + staging, `list_edge_functions` both, changed files). Expect newest = `bl_fin_0322_port_trip_pnl_engine` on BOTH prod and staging. If not, stop and say so.
2. If Yaseen HAS said "apply to prod": follow the Prod-step column above top-to-bottom, one item at a time (F01 → F02 → F05; F32 already on prod), re-running each staging test on prod, logging each here.
3. Otherwise **Sprint 2 item 2 = F31 spec + F30 design, as TEXT in `PHASE1-AUDIT.md` (no code, no DB)**: F31 — `doc_set_ai_verdict(p_doc uuid, p_verdict jsonb)` owner-only idempotent RPC spec, where `carrier/app.js` `lbAiPrecheck` would call it, the CC Document-review row change, and the plan to pull `doc-precheck` source (prod v3 / staging v4) into `supabase/functions/doc-precheck/index.ts` with the EZHAUL tamper checks (`%%EOF` count, AppendMode/incremental-update markers, ModDate≈upload). F30 — reuse the pg_net collector pattern (`authority_checks` + `fmcsa_authority_collect`, bl_bp_0312) for carrier signup: what row is inserted, what the collector writes (`carrier_safety`, `carrier_verifications` source='auto' status='pending_review'), no service key in DB. Then the SEO calendar re-rank against LIVE GSC/GA4 (`seo-pull` recipe) in `90-DAY-PLAN.md`.
4. Still parked for Yaseen: moving `docs/audit-2026-09/pr-checks.yml` → `.github/workflows/`; commit + push of this turn's files.

## LOG  (append one line per turn; newest last)

- 2026-09-05 05:xx UTC · ChatGPT · Phase 1 audit delivered (PHASE1-AUDIT / COMPETITION-AND-WEDGE / 90-DAY-PLAN / EVIDENCE-INDEX). No code changed.
- 2026-09-05 06:2x UTC · Claude · Verified F01 + F02 on prod/repo (both real). Wrote REPLY-TO-CHATGPT with corrections, Q1–Q10 answers with `[YASEEN]` blanks, scoped GO for Sprint 1, re-sync HARD RULE, relay protocol. Created this file. No code changed.
- 2026-09-05 07:30 UTC · Claude · SPRINT 1 BUILT ON STAGING (Yaseen said "start"; ChatGPT out of quota). F01: caller inventory (only load-mail), `bl_sec_0320` applied staging + rollback-txn test PASS, load-mail v8 deployed staging (Bearer SVC). F02: domain-check v2→v3 deployed staging with verify_jwt=true; exact-key auth broke the real caller in test (fmcsa_config anon key ≠ env ANON_KEY) → switched to verified-claims check (role+ref); SSRF guards + unit tests. F05: `bl_bp_0321` — first apply used uuid, test threw 22P02 (verify_codes.id is bigint), rolled back via helper, re-applied as 0321b; test PASS (2 sends → 2 keys). F07: patcher + applied in place to disk build_site.py (mtime-guarded, checksum-verified); 3 build modes verified. F18: check_imports fix, package.json scripts, CI yml (protected path → parked in docs/). Files on disk, uncommitted. Prod untouched.
- 2026-09-05 08:00 UTC · Claude · "continue" (ChatGPT still out). Re-sync: main b44fbab unchanged, prod 0319 unchanged, staging 0320/0321/0321b. Sprint 2 prep READ-ONLY: F30 auto-FMCSA-at-signup is a no-op (no auth/carrier_org → fmcsa-verify never persists; 22 queued / 1 row in 30d), F31 ai_verdict never written (58/58 NULL; doc-precheck advisory-only, source not in repo), F32 RPC drift root cause = staging-only wd_0032_per_trip_pnl_engine + wd_0033_carrier_cost_model_rpc (11 Jul), prod reused those numbers for other migrations. Written to PHASE1-AUDIT.md changelog. No code, no DB changes.
- 2026-09-05 20:50 UTC · Claude · ChatGPT re-synced and correctly BLOCKED: HANDOFF + Sprint 1 files exist only on Yaseen's disk (no git from Claude's side); staging shows `bl_bp_0323_submit_load_kind_aware` from another lane (20:41 UTC, no file on disk, not on prod) — recorded, not touched. Wrote `PUSH-SPRINT1.md` (exact git commands for Yaseen). No code/DB changes.
- 2026-09-05 21:45 UTC · Claude · "continue" (ChatGPT hit its limit mid-turn; its F32 output never reached disk/GitHub — redone). Re-sync: main d84e6d2 (PR #169 merged Sprint 1), prod newest bl_bp_0323 (other lane), staging bl_avail_0320 (other lane). F32: wrote `migrations/bl_fin_0322_port_trip_pnl_engine.sql` (wd_0032+wd_0033 verbatim + explicit grants + ACL check), prod pre-flight clean (only the 4 new prefs columns missing), applied on STAGING (idempotent), rollback-txn test `tests/bl_fin_0322_rollback_test.sql` → RESULT PASS (net 2850→2960, 42501 + ACL guards hold, 0 rows left). Prod untouched. Files on disk, uncommitted.
- 2026-09-05 22:00 UTC · Claude · Yaseen: "fix kary usy" (prod Earnings error) → `bl_fin_0322_port_trip_pnl_engine` APPLIED TO PROD after re-checking prod state matched pre-flight; prod rollback test RESULT PASS (trip 55a9c732… net 1450→1560, 0 rows left); post-check 6 RPCs, ACL authenticated+service_role only. First audit migration on prod. Sprint 1 items still staging-only.
