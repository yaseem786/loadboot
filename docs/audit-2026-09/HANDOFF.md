# AUDIT HANDOFF — the shared baton between Claude and ChatGPT

**Rule:** Whoever gets "continue" from Yaseen reads THIS file first (after the re-sync header), works the `NEXT ACTION`, then rewrites the `CURRENT STATE` block and appends one `LOG` line before stopping. Never work from memory of an earlier turn — the other assistant may have moved things since. If this file and the repo/DB disagree, the repo/DB wins; fix the file.

Both assistants follow the same guardrails (`docs/CHATGPT-AUDIT-PROMPT.md`), the same sprint plan (`90-DAY-PLAN.md`), the same findings (`PHASE1-AUDIT.md`), and the same re-sync hard rule (`REPLY-TO-CHATGPT-phase1-review.md` → "HARD RULE"). Staging first; prod only on Yaseen's explicit "apply to prod" / "deploy"; Yaseen sends all personal carrier/broker messages.

**Standing content rule (Yaseen, 5 Sep):** every SEO/content decision must start from LIVE Google Search Console query/page data and GA4 — never from guessed keywords. Recipe: `seo-pull` edge fn via `net.http_post` → poll `net._http_response`, `dimensions:['page'|'query'], rowLimit:500` (see project memory `seo_measurement.md`). Applies to the 90-day SEO calendar in `90-DAY-PLAN.md` — re-rank it against real queries before writing anything.

---

## CURRENT STATE  (rewrite this whole block every turn)

- **Updated:** 2026-09-05 20:50 UTC by Claude
- **Sprint:** 1 — boundaries (days 1–14). Status: **ALL 5 ITEMS BUILT + VERIFIED ON STAGING. Nothing on prod yet. Waiting for Yaseen: review → commit/push → "apply to prod" / "deploy" per item.** Sprint 2 prep (read-only) DONE this turn → F30/F31/F32 in `PHASE1-AUDIT.md`.
- **Working branch:** none yet — Claude has no git access to Yaseen's machine (device shell down), so every Sprint 1 file was written straight to Yaseen's DISK copy, UNCOMMITTED. **ChatGPT cannot see them until Yaseen pushes** (his 20:45 UTC re-sync correctly reported HANDOFF.md missing from main). Yaseen runs the `git` block in `docs/audit-2026-09/PUSH-SPRINT1.md` → branch `audit/s1-boundaries` on GitHub → then ChatGPT reviews.
- **main at start of turn:** `b44fbabe12ac1841b2b61c042eb042f760b8ae5f` — disk `build_site.py` was already AHEAD of main (another lane's copy edits, 06:07 + 07:20 UTC) and was patched IN PLACE with `expectedMtimeMs` guards; checksum-verified after write.
- **Migrations high-water mark:** prod `bl_bp_0319` (untouched) · staging `bl_bp_0319` + **`bl_sec_0320_email_ingest_service_only`** + `bl_bp_0321_agent_confirm_resend_idem` (superseded, rolled back) + **`bl_bp_0321b_agent_confirm_resend_idem_bigint`** (live) + **`bl_bp_0323_submit_load_kind_aware` (20:41 UTC, ANOTHER LANE — not audit work; no migration file on disk yet; not on prod; do not touch, do not reuse 0323; F32 port stays `bl_fin_0322`)**
- **Edge fns:** staging `load-mail` **v8**, `domain-check` **v3 (verify_jwt=true)** · prod `load-mail` v5-slot/"v7" source, `domain-check` v1 (verify_jwt=false) — unchanged
- **Sprint 1 items:**
  | Item | Status | Verified how | Prod step (Yaseen's word needed) |
  |---|---|---|---|
  | F01 caller inventory | done | only caller = edge fn `load-mail` (inbound-mail → load-mail → RPC, service key); 0 SQL callers, 0 cron, 0 app code | — |
  | F01 `bl_sec_0320` (guard + service-role-only grants) | done on STAGING | rollback-txn: authenticated & anon → `{"error":"forbidden","code":"LB403"}`, 0 rows written; service_role → row + delivery created; grants = postgres+service_role | 1) deploy `load-mail` v8 to prod FIRST (adds `Authorization: Bearer SVC`; v7 sent apikey only) 2) `apply_migration bl_sec_0320…` on prod 3) re-run the same rollback test on prod |
  | F02 `domain-check` v2/v3 (auth + SSRF) | done on STAGING | fake JWT → 401 at gateway; no auth → 401; real caller (fmcsa_config anon key) → full result, same shape; redirects capped (gmail 3 hops ok); `ipIsPrivate` 17/17 unit cases | deploy `supabase/functions/domain-check/index.ts` to prod with **verify_jwt=true**; then run one `shipper_business_start` on a real shipper org to confirm the collector still reads it |
  | F05 `bl_bp_0321` (resend idempotency per code) | done on STAGING (as 0321b) | rollback-txn: 2 sends → 2 deliveries with distinct keys (code ids), 2 codes total, 1 live. First attempt used uuid → 22P02; corrected to bigint (verify_codes.id) | `apply_migration` from `migrations/bl_bp_0321_agent_confirm_resend_idem.sql` (already bigint) on prod |
  | F07 build default → staging | done ON DISK (in place) | `python build_site.py` with no CONTEXT → exit 2 + clear message; `LOADBOOT_STAGING_ANON_KEY=x` → BUILD OK staging-bound; `LOADBOOT_CONTEXT=production` → BUILD OK prod-bound | none (local behaviour). ⚠ Yaseen's local flow changes: set `LOADBOOT_STAGING_ANON_KEY`, or `LOADBOOT_CONTEXT=production` for the old behaviour |
  | F18 import checker + dead scripts + CI | done ON DISK | `check_imports.py` PASS (dynamic destructured import recognised); `check_esm_syntax.sh` 149/149; `package.json` dead `test:pwa`/`test:all` removed, `check`, `check:syntax`, `check:imports`, `build:staging` added; CI yml runs syntax + imports + staging-bound build + "not prod-bound" assert | **`.github/workflows/` is a protected path for remote tools** → the workflow sits at `docs/audit-2026-09/pr-checks.yml`; Yaseen moves it to `.github/workflows/pr-checks.yml` and (optionally) adds repo secret `LOADBOOT_STAGING_ANON_KEY` |
- **Files written to disk this turn (uncommitted):** `migrations/bl_sec_0320_email_ingest_service_only.sql`, `migrations/bl_bp_0321_agent_confirm_resend_idem.sql`, `supabase/functions/load-mail/index.ts` (NEW — v8; source was never in the repo), `supabase/functions/domain-check/index.ts` (v2), `build_site.py` (F07 block, lines ~42–52), `package.json`, `scripts/check_imports.py`, `docs/audit-2026-09/apply_f07_build_default.py`, `docs/audit-2026-09/pr-checks.yml`, this file.
- **Blocked on Yaseen:** `[YASEEN: …]` answers in REPLY-TO-CHATGPT (Q2/Q3/Q4/Q6/Q7/Q8/Q10); prod steps above; moving the CI yml; commit + push.
- **Do NOT touch (Sprint 1 rule):** `app/carrier/app.js`, `app/shared/api.js`, outreach engine/schedule, auth flows, DNS/email identity, payments, agreements, CC nav.
- **Open side-threads (not sprint work):** GABE LOGISTICS re-uploads pending (corrected W-9 + COI with LoadBoot LLC holder) → approve both, set COI `expiry_date=2026-12-01`, activate. Pending deploy stack on `main` is Yaseen's.

## NEXT ACTION  (one concrete step — the thing "continue" means)

1. Re-sync header (main sha, both migration lists, `list_edge_functions` both envs, changed files).
2. If Yaseen HAS said "apply to prod" for Sprint 1: follow the "Prod step" column top-to-bottom, one item at a time, re-running each staging test on prod, and log each here.
3. Otherwise start **Sprint 2 item 1 = F32 port of the P&L engine** (staging → prod parity for the carrier Earnings screen), STAGING-side prep only until told: (a) `select statements from supabase_migrations.schema_migrations where name in ('wd_0032_per_trip_pnl_engine','wd_0033_carrier_cost_model_rpc')` on staging → save as `migrations/bl_fin_0322_port_trip_pnl_engine.sql` with a header; (b) diff every table/column it references against PROD (`information_schema.columns` for trips, loads, fin_*); (c) write the rollback-txn test (create trip_finance_items for one existing prod trip inside `DO … raise`), but do NOT run anything on prod. Then F30 design note (collector-pattern reuse) and F31 RPC spec — both as text in `PHASE1-AUDIT.md`, no code, until Yaseen approves Sprint 2.
4. Still parked for Yaseen: `[YASEEN]` blanks; moving `docs/audit-2026-09/pr-checks.yml` → `.github/workflows/`; commit + push of Sprint 1 files.

## LOG  (append one line per turn; newest last)

- 2026-09-05 05:xx UTC · ChatGPT · Phase 1 audit delivered (PHASE1-AUDIT / COMPETITION-AND-WEDGE / 90-DAY-PLAN / EVIDENCE-INDEX). No code changed.
- 2026-09-05 06:2x UTC · Claude · Verified F01 + F02 on prod/repo (both real). Wrote REPLY-TO-CHATGPT with corrections, Q1–Q10 answers with `[YASEEN]` blanks, scoped GO for Sprint 1, re-sync HARD RULE, relay protocol. Created this file. No code changed.
- 2026-09-05 07:30 UTC · Claude · SPRINT 1 BUILT ON STAGING (Yaseen said "start"; ChatGPT out of quota). F01: caller inventory (only load-mail), `bl_sec_0320` applied staging + rollback-txn test PASS, load-mail v8 deployed staging (Bearer SVC). F02: domain-check v2→v3 deployed staging with verify_jwt=true; exact-key auth broke the real caller in test (fmcsa_config anon key ≠ env ANON_KEY) → switched to verified-claims check (role+ref); SSRF guards + unit tests. F05: `bl_bp_0321` — first apply used uuid, test threw 22P02 (verify_codes.id is bigint), rolled back via helper, re-applied as 0321b; test PASS (2 sends → 2 keys). F07: patcher + applied in place to disk build_site.py (mtime-guarded, checksum-verified); 3 build modes verified. F18: check_imports fix, package.json scripts, CI yml (protected path → parked in docs/). Files on disk, uncommitted. Prod untouched.
- 2026-09-05 08:00 UTC · Claude · "continue" (ChatGPT still out). Re-sync: main b44fbab unchanged, prod 0319 unchanged, staging 0320/0321/0321b. Sprint 2 prep READ-ONLY: F30 auto-FMCSA-at-signup is a no-op (no auth/carrier_org → fmcsa-verify never persists; 22 queued / 1 row in 30d), F31 ai_verdict never written (58/58 NULL; doc-precheck advisory-only, source not in repo), F32 RPC drift root cause = staging-only wd_0032_per_trip_pnl_engine + wd_0033_carrier_cost_model_rpc (11 Jul), prod reused those numbers for other migrations. Written to PHASE1-AUDIT.md changelog. No code, no DB changes.
- 2026-09-05 20:50 UTC · Claude · ChatGPT re-synced and correctly BLOCKED: HANDOFF + Sprint 1 files exist only on Yaseen's disk (no git from Claude's side); staging shows `bl_bp_0323_submit_load_kind_aware` from another lane (20:41 UTC, no file on disk, not on prod) — recorded, not touched. Wrote `PUSH-SPRINT1.md` (exact git commands for Yaseen). No code/DB changes.
