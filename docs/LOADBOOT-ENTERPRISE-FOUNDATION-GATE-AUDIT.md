# LOADBOOT — ENTERPRISE FOUNDATION GATE — AUDIT (generated)

**Generated:** 2026-07-01T05:07:35Z · **Project:** rwscphuhpjoudvljvmdk (production) · **Staging:** snslhvmkjusozgjelghi
**Machine-generated by `scripts/generate_gate_artifacts.py`, validated against
`evidence/gate/live-source-evidence.json` (captured 2026-07-01T05:07:35Z). Do not hand-edit totals.**

## VERDICT

`LOADBOOT ENTERPRISE FOUNDATION GATE: FAIL`

**Gate summary: PASS 10 / PARTIAL 0 / BLOCKED 2 / FAIL 0 of 12.**
**Live counts:** 240 cc_* RPCs / 77 private tables / 51 modules / 30 flags / 8 edge functions / anon surface 5.

## Named gates (review items)

- **SETTLEMENT MAKER CHECKER GATE: PASS** (evidence: maker_checker_matrix)
- **SOURCE-CONTROL REPAIR GATE: PASS** (evidence: security_migrations)
- **SOURCE-OF-TRUTH CONSISTENCY GATE: PASS** (evidence: counts)
- **ANONYMOUS LOAD-SURFACE GATE: PASS** (evidence: pocket_loads_acl)
- **POD BACKEND SECURITY MATRIX GATE: PASS** (evidence: pod_backend_matrix)
- **POD UI AND REVIEW GATE: NOT-RUN** (evidence: -)
- **AUTHENTICATED PORTAL MOBILE PERSONA GATE: NOT-RUN** (evidence: -)
- **PACKAGE-WIDE REPRODUCIBLE VERIFICATION: PASS** (evidence: counts)

## Gate conditions (single canonical list)

| # | Condition | Status | Evidence | Note |
|---|---|---|---|---|
| 1 | All generated artifacts agree (source-of-truth consistency) | **PASS** | counts | Generated deterministically; consistency + evidence-ref validation asserted at build. |
| 2 | Capability registry current | **PASS** | counts | Regenerated from live counts (240 RPCs / 77 tables / 51 modules). |
| 3 | Anonymous Pocket RPC exposure resolved | **PASS** | pocket_loads_acl | cc_pocket_available_loads: anon 401, carrier-eligibility enforced (cuh). Anon SECURITY DEFINER surface = 5, all public-by-design. |
| 4 | Security migrations source-controlled | **PASS** | security_migrations | migrations/ct-waveBI-security-gate-repair/{cuf,cug,cuh,cui,cuj,cuk,cul,cum,cun,cuo,cup}.sql written to repo + pushed via desktop bridge; Supabase ledger versions recorded. |
| 5 | Real POD frontend upload + review workflow proven | **BLOCKED** | - | FRONTEND NOW BUILT + BACKEND MATRIX PROVEN. Built: Pocket app POD panel (file/camera picker, type+size validation, preview, remove/replace, upload state, success, network-failure retry, existing-version + review-status + rejection-reason + resubmit); Carrier Portal POD panel (desktop drag-and-drop + same validation/states); Command Center 'POD Review Queue' (carrier/route/delivery context, signed private preview, approve, reject-with-required-reason). Backend hardened + proven on staging: cc_pocket_upload_pod re-validates carrier org, trip ownership, trip state, MIME, size and the {uid}/pod/{trip}/{file} object path server-side; cc_pod_review_queue / cc_pod_signed_ref / cc_review_pod are reviewer-gated; approval emits invoice.prep_requested exactly once. tests/security/pod_backend_matrix.sql = 21/21 PASS. STILL OPEN (required, not polish): the real browser upload+review captured against a deployed site with a logged-in carrier/driver/staff session (screenshots + Playwright result under evidence/gate/pod/). The assistant cannot type passwords or reach the site via headless egress; owner runs tests/security/pod_workflow.spec.js per the runbook. Owner-executed proof required. |
| 6 | Settlement / payout-hold + maker-checker proven | **PASS** | maker_checker_matrix | 11/11 staging matrix PASS: self-approve DENIED, carrier/dispatcher DENIED, different checker approves, duplicate idempotent, stale-version DENIED, amount-change invalidates approval, payout HELD, one event, no money moved. (Carrier-portal settlement view is a minor UI follow-up.) |
| 7 | Authenticated portal mobile / persona browser tests executed | **BLOCKED** | - | TEST SYSTEM NOW BUILT + STRENGTHENED. persona_matrix.spec.js was upgraded from nav-visibility-only to real SERVER-SIDE enforcement: for each of 11 personas x 4 viewports (44 combos) it opens the correct portal, checks role-aware nav, runs a permitted RPC (success), and calls a FORBIDDEN RPC DIRECTLY against PostgREST with the persona's own token and asserts the backend denies it, plus mobile-menu / no-overflow / clean-console / no env-leak / screenshot. Added: tests/security/{personas.js, auth-setup.spec.js, pod_workflow.spec.js, .env.example, PERSONA-TEST-RUNBOOK.md}, playwright.config.js, .auth/.gitkeep + gitignores. Suite SKIPS cleanly without PERSONAS_READY + storage states (so local gates show expected skips). RBAC SEPARATION already PROVEN via SQL/JWT-simulation (11 personas). STILL OPEN (required, not polish): the owner-run authenticated matrix with ZERO skips (44 combos), JSON+HTML report + traces. Requires owner-generated per-persona storage-states (login) + egress. Owner-executed proof required. |
| 8 | FMCSA verification succeeds with official data | **PASS** | fmcsa_verify | Owner set a valid FMCSA QCMobile WebKey. Live test fmcsa-verify(dot=130113) -> HTTP 200 with real official FMCSA data (GUFFEY-SCHLENSKER DRILLING CORP, authority active, safety fields parsed). Was 403; now live end-to-end with government data. No fabricated data. |
| 9 | Production test-data governance documented | **PASS** | testdata_inventory | Full inventory + remediation plan; no records altered. Finding: all prod records except LoadBoot org are synthetic. |
| 10 | Plugin lifecycle evidence complete | **PASS** | plugin_lifecycle | Manifest+permissions+domains+secrets; install event 15 / uninstall event 16; kill-switch off/on on ga4-insights; 5 pre-existing unchanged. |
| 11 | Rollback preserved | **PASS** | security_migrations | All DB changes tracked migrations with ledger versions + documented rollback/forward-fix. |
| 12 | No production leakage | **PASS** | anon_surface | build_site.py isolation gate; anon surface minimized to 5 public-by-design functions. |

## Six golden workflows

| # | Workflow | Status | Evidence |
|---|---|---|---|
| 1 | Carrier onboarding | **PASS** | 3 carrier orgs + 4 profiles |
| 2 | Verification (FMCSA) | **PASS** | fmcsa-verify HTTP 200, real FMCSA data (GUFFEY-SCHLENSKER, DOT 130113); valid WebKey |
| 3 | Load offer & assignment | **PASS** | load 92cd29be; load.assigned event |
| 4 | Trip / POD | **PARTIAL** | trip lifecycle + POD frontend BUILT (Pocket + Carrier Portal upload UI + Command Center review queue) + POD backend security matrix 21/21 PASS on staging (upload/MIME/size/path/state validation, cross-carrier & non-reviewer denials, reject-with-reason, resubmit-new-version, approve->invoice-prep-once). Owner-run browser capture of the deployed flow still pending. |
| 5 | Invoice / settlement / payout approval | **PASS** | INV-2026-00001 -> STL-2026-0001 -> approved -> paid; maker/checker enforced (11/11) |
| 6 | Marketing form -> CRM lead | **PASS** | request-a-quote -> Westlane Logistics lead; form.submitted event |

## Security posture

- Advisor ERROR-level findings: **0**.
- Anonymous SECURITY DEFINER surface: **31 -> 5** (public-by-design: cc_get_public_form, get_active_public_announcements, get_public_load_opportunities, submit_web_form, track_web_event).
- Migrations: cuf (20260701042300), cug (20260701043219), cuh (20260701043931), cui (20260701045324), cuj (20260701050526), cuk, cul_pod_review_workflow, cum_pocket_pod_status, cun_pod_upload_hardening, cuo_pod_queue_enrich, cup_register_pod_module.
- Live probes: {"cc_finance_analytics": "staff 200 / anon 401", "cc_pocket_available_loads": "staff 200 / anon 401", "cc_decide_settlement": "authenticated-only (re-revoked in cuk)", "fmcsa-verify": "HTTP 200 real FMCSA data (valid WebKey)", "send-sms": "credentials valid (Twilio auth OK)"}.

## Remaining blockers (owner / persona)

- RESOLVED: FMCSA_WEBKEY valid (live 200, real data); Twilio credentials valid; 11 staging personas created + wired; settlement maker/checker 11/11; anon surface 31->5; source-controlled migrations.
- OPEN (condition 5) POD UI+REVIEW: frontend is BUILT (Pocket + Carrier Portal upload UI + Command Center review queue) and the backend matrix is 21/21 PASS on staging. Remaining owner step: git push -> Netlify staging preview -> log in as carrier/driver/staff -> run tests/security/pod_workflow.spec.js to capture the real upload+review (screenshots + Playwright result under evidence/gate/pod/). Assistant cannot type passwords or reach the site via headless egress.
- OPEN (condition 7) AUTH PERSONA TESTS: strengthened persona_matrix.spec.js (server-side denial proof) + auth-setup.spec.js + runbook are BUILT and skip cleanly without storage states. Remaining owner step: generate per-persona storage-states (PERSONA_* env) then run with PERSONAS_READY=1 for ZERO skips (44 combos) + JSON/HTML report. RBAC separation already proven via SQL.
- PITR (FREE plan) — manual backup runbook provided; out of Foundation Gate scope per owner.

## Verdict rationale (honest)

Not all 12 conditions are PASS. Every PASS above carries a validated evidence reference and nothing was fabricated; see each non-PASS condition's note for the exact blocker.
