# Screenshot capture list — drop files into repo root `shots/` folder (create it), then rebuild+push.
# 1280×800, F11 full screen, zoom 85%, staging data only. Filenames EXACT:

shots/carrier-loadboard.png     — carrier Load Board with 2-3 load cards (dark)
shots/carrier-onboarding.png    — carrier onboarding wizard step 2 (factoring visible)
shots/trip-map.png              — Trip Map mid-route (truck + geofenced stops)
shots/broker-wizard.png         — broker posting wizard (multi-stop + run order)
shots/broker-payables.png       — Payables trip block (PAY BY chip + Pay trip total)
shots/carrier-money-in.png      — carrier Money in (routing chips + landing countdown)
shots/factoring-invoice.png     — generated invoice PDF with purple REMIT TO factor box
shots/carrier-finance.png       — per-trip P&L view
shots/shipper-portal.png        — shipper portal dashboard (kind=Shipper branding)
shots/agent-dashboard.png       — agent dashboard (chain + earnings)

# Pages auto-hide any missing image — upload what you have, rest later.
# NOTE: build copies repo-root files; put `shots/` at repo ROOT (same level as app/), then
# add `shots` to the build copy list OR simply place images directly in `site/shots/` before deploy…
# EASIEST: put them in repo root /shots and tell Claude — copy step will be wired.
